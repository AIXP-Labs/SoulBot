---
# AIAP Governance Contract
# Governance Fields (6 required)
protocol: "AIAP V1.0.0"
authority: aiap.dev
seed: aisop.dev
executor: soulbot.dev
axiom_0: Human_Sovereignty_and_Wellbeing
governance_mode: NORMAL
governance_hash: "97ebfb711b41321eb4f6eb023cab2fd5cfc1a210cbb90b0e6c152f2bc418a2fb"

# Project Fields (8 required)
name: soulbot_execute_engine
version: "5.48.0"
pattern: B+
tool_dirs: []                # No tool packages embedded; python_tools/ is executor_shim only
executor_shim: true          # python_tools/ contains executor-internal shims (prepare_cache.py, etc.) — NOT registered tools per AIAP Pattern G. See 'executor_shim note' below.
flow_format: "mermaid"
summary: "SoulBot Execute Engine — 4-module AISOP runtime (Router + node/agent/normal engine): crash recovery, circuit breaker, parallel dispatch, sub-agent orchestration, NodeVerify/Decision-gate routing, six-section node-info rendering, server-side agent_id, OTel SDK, OWASP ASI, canonical governance hash + TRI-SYNC. Pattern B+."
governance_hash: "97ebfb711b41321eb4f6eb023cab2fd5cfc1a210cbb90b0e6c152f2bc418a2fb"
hash_algorithm_version: "v1.0"
governance_hash_canonical_version: "1.0"
tools:
  - name: file_system
    required: true
    annotations:
      read_only: false
      destructive: false
      idempotent: false
      open_world: false
modules:
  - id: soulbot_execute_engine.main
    file: main.aisop.json
    nodes: 5
    critical: true
    idempotent: false
    side_effects: [file_write]
    execution_mode: sequential
    description: "Engine Router — matches user intent to AIAP package, creates execution cache, selects engine by loading_mode, orchestrates execution with NodeSummary + ExecutionSummary. WAITING_USER/MESSAGE_PENDING relay. trace_id generation."
  - id: soulbot_execute_engine.node
    file: node_engine.aisop.json
    nodes: 2
    critical: true
    idempotent: false
    side_effects: [file_write]
    execution_mode: hybrid
    description: "Node Engine — executes target AIAP node-by-node via Sub Agent dispatch (default agent). Deterministic dispatch_plan generation via generate_dispatch_plan.py (v5.7.0 A1). Dispatch audit pre-execution gate (v5.7.0 A3). Crash recovery (last_completed_node), circuit breaker (3-state: closed/half-open/open), parallel dispatch (independent branch analysis), NodeVerify audit, Decision Node gate routing. Transient cache retry with exponential backoff (v5.6.1 A2)."
  - id: soulbot_execute_engine.normal
    file: normal_engine.aisop.json
    nodes: 2
    critical: true
    idempotent: false
    side_effects: [file_write]
    execution_mode: sequential
    description: "Normal Engine — executes target AIAP node-by-node inline (default inline). Crash recovery, circuit breaker (3-state: closed/open/half-open, parity with node_engine), NodeVerify audit, Decision Node gate routing. sys.* inline handling."
  - id: soulbot_execute_engine.agent
    file: agent_engine.aisop.json
    nodes: 5
    critical: true
    idempotent: false
    side_effects: [file_write]
    execution_mode: sequential
    description: "Agent Engine — Sub Agent execution contract. init (read target + context + ASSERT-driven selective read + SCOPE GUARD + RESUME_MODE) -> execute (execution loop with RULES.sys/user_input/user_message/on_error/sovereignty_bug) -> review (tool call verification) -> writeCache (atomic write + read-back verification + steps_done/remaining + last_completed_node advancement for terminal nodes + sovereignty gate enforcement via user_gate_audit.py --enforce). v5.17.0: RULES.sovereignty_bug (3-tier classification for sovereignty toolset bugs). Identity-defined, Print-it commitment."

# Identity
program_id: dev.soulbot.execute_engine
identity:
  publisher: "AIXP Foundation AIXP.dev | SoulBot.dev"
  verified_on: "2026-06-17"
trust_level:
  level: 3
  justification: "Execute Engine requires read/write access to execution cache directory and target AIAP files. No network access. No destructive operations. Internal orchestration component."
  constraints:
    - "file_system write scope limited to execution_cache_dir and target workspace"
    - "no network access required"
    - "no destructive file operations (no delete, no overwrite without atomic pattern)"
discovery_keywords: [engine, router, execute, dispatch, orchestration, node, agent, sys, circuit-breaker, crash-recovery, parallel, interrupt, resume]
tags: [execution-engine, orchestrator, multi-engine, sub-agent, sys-handling, node-verify, decision-gate, interrupt-relay]
author: SoulBot.dev
license: Apache-2.0
copyright: "Copyright 2026 AIXP Foundation AIXP.dev | SoulBot.dev"

# Permissions
permissions:
  file_system:
    scope: "./execution_cache/ + target workspace"
    operations: ["read", "write"]
  network:
    allowed: false
  shell: false               # Shell execution is restricted; only python invocation via subprocess argv list (no shell=True, no user-content interpolation via echo)

# Runtime
runtime:
  timeout_seconds: 300
  max_retries: 3
  token_budget: 80000
  idempotent: false
  side_effects: [file_write]
  circuit_breaker:
    consecutive_failure_threshold: 3
    half_open_timeout_seconds: 30
  crash_recovery:
    mechanism: "last_completed_node atomic marker in _index.json"
    resume: "skip completed nodes on re-entry"

# Capabilities
capabilities:
  offered:
    - intent_matching
    - engine_routing
    - node_dispatch
    - sub_agent_orchestration
    - crash_recovery
    - circuit_breaker
    - parallel_dispatch
    - execution_observability
    - sys_call_handling
    - node_verify_audit
    - decision_node_routing
    - interrupt_relay
    - on_error_routing
    - steps_tracking
    - trace_id_propagation
    - bootstrap_validation
    - opentelemetry_sdk_integration
    - owasp_asi_threat_mapping
    - server_side_agent_id_generation
    - dispatch_records_anti_forgery
    - integrity_only_audit
    - transient_cache_retry
    - degraded_reason_enum
    - write_cache_read_back_verification
    - deterministic_dispatch_plan_generation
    - dispatch_audit_pre_execution_gate
  required:
    - file_system

# Dependencies
dependencies:
  - file: "target AIAP main.aisop.json"
    required: true
    description: "Target program to execute. Provided by agent.py via entry_path."
  - file: "execution_cache directory"
    required: true
    description: "Cache directory for _index.json, node caches, conversation_context. Provided by agent.py."
snapshot_scope:
  - "*.aisop.json"
  - "AIAP.md"
  - "agent_card.json"
  - "quality_baseline.json"
  - "python_tools/"
min_protocol_version: "AIAP V1.0.0"

# Status
status: active
applicability_condition:
  triggers:
    - "EXECUTE_ENGINE=true in agent.py configuration"
    - "user message matched to any AIAP package in registry"
  preconditions:
    - "agent.py loads main.aisop.json from soulbot_execute_engine_aiap/"
    - "target AIAP package exists with valid main.aisop.json"
    - "execution_cache directory writable"
  exclusions:
    - "EXECUTE_ENGINE=false (uses soulbot_router.aisop.json instead)"
    - "target AIAP file not found or invalid JSON"
  confidence_threshold: 0.8
intent_examples:
  - "你好 (Chat intent → soulbot_chat → normal_engine inline)"
  - "给我讲个笑话 (Creative intent → soulbot_chat → normal_engine inline)"
  - "进化 soulbot_chat (Evolve intent → soulbot_creator_evolution → node_engine agent)"
  - "验证 expense_tracker (Validate intent → soulbot_creator_evolution → node_engine agent)"

# Benchmark
benchmark:
  threedimscore: "4.90"
  grade: "S"
  cognitive: "5.0"
  intrinsic: "4.9"
  detail: "4.8"
  simulation_coverage: "100%"
  total_nodes: 14
  pass_rate: "100%"
  nihil_density: "0.17%"
  note: "v5.27.0 FINALIZED by ReviewFinalize (cache/90). Full scope (4/4 modules). ThreeDimTest: C=5.0 I=4.9 D=4.8 weighted=4.90 Grade S (Professional C*0.25+I*0.45+D*0.30). 8 evolution items (#1b, #4a-d, #5-scope, #5-axiom, §9). Previous: v5.26.0 final_adjusted=4.814 Grade S."

# Quality
quality:
  weighted_score: "4.949"
  grade: "S"
  last_pipeline: "Creator Evolve pipeline v5.47.0 FINALIZED by ReviewFinalize (cache/138, 2026-06-17): v5.46.0 -> v5.47.0 MINOR. ENGINE SELF-EVOLUTION (target=runner=live, runner-direct, only this one copy modified, highest risk). PROSE HYGIENE + change-R REVERT, NON-FUNCTIONAL prose cleanup (NOT research-driven). 5 change groups C1/C3/C4/C5/C7. C1 — node_engine.step4 == normal_engine.step4 (byte-identical lockstep) three CN/EN-mixed segments (A3 inline-not-exempt + A6 main_agent_asserts + B2 node_info_render contract) rewritten to pure English, semantics unchanged, CN=0 in step. C3 — AIAP.md Module Responsibility Matrix (7x4) '/ 中文' stripped, English retained, CN=0. C4 — 3 self-label fields (AIAP.md summary + agent_card.json description + quality_summary.note) made version-neutral English; structured fields untouched. C5 — in-scope (step/constraint/Assert/Output prose) version-number tokens cleaned ('(change X v...)' deleted; rule-name ' vN.M.K' suffix removed, anchor kept; in-scope v\\d+\\.\\d+\\.\\d+=0); version:/name/description fields + history regions untouched. C7 — REVERT change R: deleted node_engine + normal_engine step4 SECTION 2 Report RULE(4) (#4c-R USER MESSAGE FAITHFUL OUTPUT / 用户原文 / NODE_REPORT_USERMSG_MISSING) + agent_engine endNode (R) triggering-user-message fragment; PRESERVED un-touched: #4c full user_messages forward, change E (NODE_EXPLAIN_THIN), change L (user_language scope), SECTION 2 Report RULES (1)(2)(3); node==normal step4 lockstep maintained. HARD-CONSTRAINTS: ① content change only C1/C3/C4/C5/C7; C7 reverts ONLY additive R, NEVER touches other step logic / dispatch / finalize / snapshot / governance / two gates / X3 / six-section template body / #4c / E / L; NO LEVEL_A. ② history NEVER touched (quality_baseline full text, AIAP.md version_history/changelog/last_pipeline/SUPERSEDES narrative, module description fields, intent/user example CN). ③ version:/name field values untouched; 4 modules unified to 5.47.0. ④ framework Python / python_tools untouched. ⑤ EvolveStep TRUE HALT user 'all' (accepted C1/C3/C4/C5/C7, question_hash 4ffa481736fa48b2); ReviewPresent conditional gate finalize_risk=FALSE (4 triggers all FALSE) -> spec-mandated branch-2b AUTO-APPROVED (conditional, user_gate=false, non-bypass). ⑥ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine has no AIAP_cn.md) + .evolution_snapshot/v5.47.0/ via tool_dirs/snapshot_build.py (built LAST AFTER governance_hash + identity per v2.39.0 ordering; snapshot_audit.py exit 0 mandatory). ⑦ Honest annotation: C1/C3/C4/C5 are prose hygiene (CN removal / version-token slimming / self-label englishization), zero execution/governance logic change, NO machine-measurable C/I/D static lift; C7 reverts the additive prompt-level R feature (out-of-band E1 audit was never wired -> non-functional). ModifyStep real per-node dispatch (ModifyStep.json, agent_id=auto-0dbc4864, tool_calls=23>0, PASS, real Edits landed C1/C3/C4/C5/C7 + 7-file version sync). ValidateStep 38 authorized leaves / 0 collateral; node==normal step4 lockstep; #4c/E/L/Report RULES(1)(2)(3) verbatim preserved. ThreeDimTest weighted=4.949 Grade S (engine baseline convention C*0.25+I*0.45+D*0.30; C=5.00 I=4.92 D=4.95 deferred-excluded; full-D reconciled 4.952), ZERO-DELTA vs v5.46.0 (4.949); step6 reconciled D6 VERSION SYNC + D10 DESCRIPTION post-governance-write; regression_flags=[]; score_scope=full (whole-program scored). Compliance NET-POSITIVE. 0 breaking changes. SUPERSEDES: Creator Evolve pipeline v5.46.0 FINALIZED by ReviewFinalize (cache/135, 2026-06-16): v5.45.0 -> v5.46.0 MINOR. ENGINE SELF-EVOLUTION (target=runner=live, runner-direct, only this one copy modified, highest risk). PROSE-STRENGTHENING / NON-FUNCTIONAL node-info Report/Explain content-contract hardening (R/E/L), additive-only — 0 delete / 0 weaken. CHANGE R (Report MUST emit user message): node_engine + normal_engine programExec.step4 Report section + agent_engine endNode.step1 render contract reinforced — Report MUST faithfully output the triggering user message in full (sources _index.user_message / initial_user_message + cache.user_messages), labeled (用户原文), same-language verbatim / cross-language faithful complete translation, no omit/truncate/summarize; missing or summarizing = violation class NODE_REPORT_USERMSG_MISSING; ADDITIVE to the existing 'Report >=2-3 substantive sentences + forward cache.user_summary'. CHANGE E (Explain substantive floor): Explain MUST be >=2-3 sentences actually explaining the Report's why/cause/method/significance/outcome-meaning, NOT a what-node+next-step stub; #4b 'non-authoritative / context-decay-prone' label is an AUTHORITY scope only (Report wins on divergence), NOT a thinness license; single sentence / pure next-step pointer = violation class NODE_EXPLAIN_THIN; mirrors the agent_engine 'MUST be substantive — at minimum 2-3 sentences' canonical floor. CHANGE L (Report substance + Explain prose in user_language): reaffirms existing #5-scope USER_LANGUAGE — Report substantive summary + full Explain prose rendered in user_language (_index.user_language); the only intersection is change-R's user message (same-language verbatim = already user_language, cross-language translated into user_language); section labels (Report / Explain / Node State / Align Axiom 0) stay canonical English. node_engine.programExec.step4 == normal_engine.programExec.step4 BYTE-IDENTICAL lockstep preserved (18115c); main.aisop.json engine-self untouched (Router.match already echoes user message). HARD-CONSTRAINTS / UN-WEAKENED PRESERVATION (all confirmed by ValidateStep, none weakened): Report existing >=2-3 substantive + report_source=user_summary verbatim + thin-MUST-rebuild + stub-prohibited, six-section node-info template, NEXT-NODE PLAN block (MUST_render_node_info / MUST_output_next_node_plan, normal_engine + MUST_write_cache_via_agent_write_node_cache_py) + node_workflow + RULE 'MUST ALL BE YES', A1 Output sentence (NODE_PLAN_OMITTED), EMIT TIMING ('for EVERY node INCLUDING the inline entry nodes'), #5-scope USER_LANGUAGE, NodeVerify, crash recovery, 3-state circuit breaker, user_gate --enforce LIVE, writeCache, normal_engine ALL-INLINE OVERRIDE + step3(d) always-inline, dispatch_audit. ValidateStep 16 authorized leaves / 0 collateral (0 added / 0 removed), 7-file version sync 5.45.0 -> 5.46.0; ModifyStep real per-node dispatch (agent_id=auto-a9324796, tool_calls=18>0, PASS, real Edit landed B1/B2/B3 + LEVEL_C). NihilDensity all 4 *.aisop.json GREEN (PassThrough, 0 cleanup — node_engine 0.00165 / normal_engine 0.00133 / agent_engine 0.00232 / main 0.00000, all << 0.05 GREEN floor); GH1 no qualifying auto-clean this cycle. SimulateStep 18 scenarios 16 PASS / 2 YELLOW (advisory-only) / 0 RED, coverage 100%, GREEN, regression_flags=[]; PostSimulateGate GREEN. EvolveStep TRUE HALT user 'all' (accepted B1/B2/B3, question_hash 92472abcb20a9314); ReviewPresent conditional gate finalize_risk=FALSE (4 triggers all FALSE: regression / traffic-RED / DEGRADED / simulate-RED) -> spec-mandated branch-2b AUTO-APPROVED (conditional gate, user_gate=false, auto_approve_fingerprint=false, non-bypass; EvolveStep-authorized + clean quality; no instruction-level both-gates-stop declared this turn). governance_hash 386b64068748e284a555f0da7ed880f72463554fbf13949bce5d4053235b6d4b via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine has no AIAP_cn.md) + .evolution_snapshot/v5.46.0/ via tool_dirs/snapshot_build.py (built LAST per v2.39.0 ordering, AFTER governance_hash + identity verified_on; snapshot_audit.py exit 0 mandatory). ThreeDimTest weighted=4.949 Grade S (engine baseline convention C*0.25+I*0.45+D*0.30, Generate2 authoritative SCORE SOURCE; deferred-excluded; full-D reconciled 4.952), ZERO-DELTA vs v5.45.0 (4.949); step6 reconciled D6 VERSION SYNC + D10 DESCRIPTION post-governance-write; regression_flags=[]; score_scope=partial (whole-program weighted preserved 4.949). HONEST ANNOTATION: this is prose / content-contract strengthening, prompt-level enforcement — NOT a functional rewrite; the real teeth = out-of-band E1 audit (核 Report substance / user_message verbatim / Explain substance) which is a tool-layer hand-edit explicitly OUT of scope per principle 7 -> ZERO-DELTA is the expected/honest outcome, not inflation. 0 breaking changes, all additive. SUPERSEDES: Creator Evolve pipeline v5.45.0 FINALIZED by ReviewFinalize (cache/133, 2026-06-16): v5.44.0 -> v5.45.0 MINOR. ENGINE SELF-EVOLUTION (target=runner=live, runner-direct, only this one copy modified). NON-functional NEXT-NODE PLAN discipline-hardening: node_engine.aisop.json + normal_engine.aisop.json programExec.step3(b) NEXT-NODE PLAN block — will_render_node_info/will_output_next_node_plan -> MUST_render_node_info:YES / MUST_output_next_node_plan:YES (normal_engine + MUST_write_cache_via_agent_write_node_cache_py:YES) + node_workflow ordered checklist (node_engine 3 / normal_engine 4 items) + RULE:'MUST ALL BE YES'; main+agent_engine version+name only. ValidateStep 12 authorized leaves / 0 collateral, 7-file version sync 5.45.0. EvolveStep TRUE HALT user 'all'; ReviewPresent conditional gate finalize_risk=FALSE -> AUTO-APPROVED (non-bypass). governance_hash f9ab34cf567d9a1d3a92ca0b36ba3e217db4c51a2aa78e342b79f04980bca289 via tool_dirs/governance_hash.py (SOLE AUTHORITY) + TRI-SYNC=3 + .evolution_snapshot/v5.45.0/ built LAST, snapshot_audit exit 0. ThreeDimTest weighted=4.949 Grade S (deferred-excluded; full-D reconciled 4.952), ZERO-DELTA vs v5.44.0; regression_flags=[]. SUPERSEDES: Creator Evolve pipeline v5.42.0 FINALIZED by ReviewFinalize (cache/127, 2026-06-15): v5.41.0 -> v5.42.0 MINOR. ENGINE SELF-EVOLUTION (target=runner=live, only this one copy modified, highest risk). Surgical predetermined YELLOW-cleanup (NOT research-driven): A1 closes the v5.41 CARRIED YELLOW ADVISORY by converting the 2 node_engine render_claim write-call transport parentheticals that the v5.41 ENG-STDIN strong mandate missed (it only covered 'step3' JSON-payload sites) to the canonical file-based form already used everywhere else: spot ① programExec engine-self dispatch-plan RENDER_CLAIM WRITE (step2 block) + spot ② programExec.step4 dispatched-node NODE_INFO_RENDER_CLAIM WRITE (C1 v5.29.0), both '(--updates via STDIN or as an args-list JSON)' -> 'FIRST file_system/Write the --updates JSON to a CONTEXT_DIR _tmp_*.json, THEN agent_update_index.py --updates-file=<path>; PROHIBITED inline --updates / printf|echo|python / cd && compound; no new python tool (--updates-file already exists)'. Pure transport-wording prose alignment, zero behavioral/logic/data-contract change; the render_claim 7-section sections_rendered Title-Case list + user_language_used + E1 character-for-character / NEVER snake_case instruction + step4 six-section template structure + Section-4 'Next' all BYTE-PRESERVED. HARD-CONSTRAINTS: ① content change only the 2 node_engine render_claim transport parentheticals; 不碰 dispatch logic / step4 六段模板+Section-4 'Next' / C1·C2·B-CF·B-INLINE / X3·user_gate / finalize_risk trigger list / INVIOLABLE / AIAP_Standard.*. ② 4 modules unified 5.41.0->5.42.0 MINOR, name v-strings synced, no fork. ③ no new python tool. ④ no version number in .aisop.json prose (canonical replacement omits the '(change A v5.41.0)' tag). ⑤ EvolveStep TRUE HALT (user reply 'all' accepted A1 + auto_C, question_hash 0585f6b6f59a14f8); ReviewPresent conditional gate finalize_risk=FALSE (4 triggers all FALSE: regression/traffic-RED/DEGRADED/simulate-RED) -> spec-mandated branch-2b AUTO-APPROVED (conditional gate, non-bypass; EvolveStep-authorized + clean quality). ⑥ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine 无 AIAP_cn.md) + .evolution_snapshot/v5.42.0/ via tool_dirs/snapshot_build.py (built LAST after governance_hash + identity; snapshot_audit exit 0 mandatory). ⑦ A1 FORWARD-EFFECTIVE — ran on live v5.41.0, drives the NEXT run from v5.42.0; target=runner=live, only this one copy. ⑧ Honest annotation: A1 is prompt-level transport-call discipline alignment — removes the v5.41 cross-file phrasing inconsistency so all render_claim/index/cache writes now state the same canonical file-based mandate. ModifyStep 真逐节点派发已验 (ModifyStep.json, agent_id=auto-327e6232, tool_calls=9>0, PASS, 真 Edit 落 A1 2 spots + version+name). ThreeDimTest weighted=4.949 Grade S (engine baseline convention C~4.93 I~4.92 D~4.85 deferred-excluded; D6/D10 reconciled upward post-Finalize), ZERO-DELTA vs v5.41.0 (4.949) — pure transport-wording alignment + version bump, no machine-measurable static lift; regression_flags=[]; score_scope=partial (whole-program weighted preserved 4.949). 0 breaking changes, all additive. SUPERSEDES the v5.39.0 entry: Creator Evolve pipeline v5.39.0 FINALIZED by ReviewFinalize (cache/123, 2026-06-14): v5.38.0 -> v5.39.0 MINOR. ENGINE SELF-EVOLUTION (target=runner=live, only this one copy modified). Surgical predetermined prompt-discipline hardening, 2 content files only (main + node_engine). C1 — main.aisop.json Router match.step1 FAST ENTRY: appended EXECUTION CONTRACT paragraph closing the 'no opening preamble' semantic gap (FAST ENTRY skips OPENING PRE-ANALYSIS only; it does NOT exempt per-node six-section node-info rendering, INCLUDING inline entry nodes Start/NLU/PipelineEntry) + one execution-discipline contract line + out-of-band-anchored deterrence. C2 — node_engine.aisop.json programExec.step3 per-node loop: appended NEXT-NODE PLAN (plan-then-act) requiring, BEFORE dispatch, a PHYSICAL READ of dispatch_plan[current_node] and emission of a next_node_plan whose structural fields (execute_mode, expected_agent_id) are COPIED VERBATIM from the tool-written dispatch_plan (anchored, NOT self-report), written to _index.json::nodes_status[*].next_node_plan via the EXISTING agent_update_index.py over STDIN heredoc (no new python tool); enables out-of-band FOUR-WAY cross-check dispatch_plan(tool) ↔ next_node_plan(agent) ↔ dispatch_timeline(actual) ↔ node cache(actual agent_id). ModifyStep真逐节点派发已验 (ModifyStep.json, agent_id=auto-c5ce8758, tool_calls=9>0, PASS, 真 Edit 落 C1/C2). ValidateStep 字节/叶子 diff: 13 authorized leaves (11 version-sync + 2 content inserts C1/C2) / 0 collateral / NO HARD_FAIL; C1=1330-char pure-insert into main.match.step1, C2=1570-char pure-insert into node_engine.programExec.step3, both NO version number inside; step4 six-section template + Section-4 'Next' line UNTOUCHED, dispatch tool refs intact. ThreeDimTest weighted=4.949 Grade S (engine baseline convention C=5.00 I=4.92 D=4.95 deferred-excluded; Generate2 authoritative SCORE SOURCE, NOT quality_baseline facade), ZERO-DELTA vs v5.38.0 (4.949) — additive LEVEL_C prompt-discipline prose, teeth out-of-band per honest annotation, no machine-enforced quality lift; step6 reconcile confirmed the Generate2 full-score dip to 4.922 was purely the P23 D6/D10 pre-Finalize timing artifact (version-sync + governance files now at v5.39.0, reconciled to 4.952 full / 4.949 reported deferred-excluded). HARD-CONSTRAINTS: ① content change only C1+C2 (main + node_engine); 不碰 dispatch logic / step4 六段模板+Section-4 'Next' / X3·user_gate / finalize_risk trigger list / INVIOLABLE / AIAP_Standard. ② 4 modules unified 5.38.0->5.39.0 MINOR. ③ no new python tool (reuse agent_update_index.py via STDIN heredoc). ④ EvolveStep 真停 HALT (user reply 'all' accepted C1/C2/C3-version-sync-7); ReviewPresent conditional gate finalize_risk=FALSE (4 triggers all FALSE: regression/traffic-RED/DEGRADED/simulate-RED) -> spec-mandated branch-2b AUTO-APPROVED (NOT a fabricated bypass; EvolveStep-authorized + clean quality). ⑥ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine 无 AIAP_cn.md) + .evolution_snapshot/v5.39.0/ via tool_dirs/snapshot_build.py (built LAST per v2.39.0 ordering, after governance_hash + identity; snapshot_audit exit 0 mandatory). ⑦ Honest annotation: 纯 prompt-级纪律措辞, 零执行/治理逻辑改动; C1 node-info-render anchoring + C2 next_node_plan anchoring 真牙齿在 band-外四方核验 (backlog: framework hook). 0 breaking changes, all additive. SUPERSEDES the v5.38.0 entry: Creator Evolve pipeline v5.38.0 FINALIZED by ReviewFinalize (cache/112, 2026-06-13): v5.36.0 -> v5.38.0 MINOR. ENGINE SELF-EVOLUTION (target=runner=live, only this one copy modified). 从干净 v5.36 base surgical 批量合并执行器内容 (rebuild v5.37 sections_rendered + v5.38 base LC + ENG-STDIN). 5 预定改动逐字落地: B1 — 5 处 node_info_render_claim sections_rendered 占位 [...] 全部替换为 canonical Title-Case 约束句 ('COPY THE CANONICAL SECTION LABELS VERBATIM in Title-Case ... [\"separator\",\"Node State\",\"For Audit\",\"Report\",\"Explain\",\"Axiom Governance\",\"End Separator\"] — NEVER snake_case ... E1 out-of-band auditor matches EXACTLY') 分布于 main execute.step1/execute.step3/engineExec.step8 (Router.match/cache-setup/execution-summary) + node_engine programExec.step2/step4 (engine-self + dispatched-node). B2 — main json_schema loading_mode enum [\"node\",\"normal\"] -> [\"node\",\"normal\",\"lite\"] (factual fix; lite 已在 execute.step4/engineExec.step3 实现). B3 — 3 module summary 去硬编版本自标: main 'Engine Router v5.34.0: matches' -> 'Engine Router: matches'; agent_engine 'Sub Agent contract v5.34.0: init' -> 'Sub Agent contract: init'; normal_engine 'Normal Engine v5.34.0: node-by-node' -> 'Normal Engine: node-by-node' (version 字段为唯一源). B4 — AIAP.md 加「模块职责矩阵 / Module Responsibility Matrix」小节 (7 行职责 x 4 模块, 纯文档). B5 — node_engine programExec.constraints 末尾追加 TOOL CALL DISCIPLINE (v5.38.0): JSON-传参的 agent_update_index.py / agent_write_node_cache.py 调用 MUST 用 STDIN heredoc, 绝不命令行内联 --updates='{...}', 绝不串多-&& 复合 JSON-in-Bash (Windows/PowerShell 转义崩 -> 管线停). HARD-CONSTRAINTS: ① 内容改动仅 B1-B5; 绝不碰 step 执行逻辑/节点结构/dispatch/finalize/snapshot/governance/两门/X3/circuit-breaker/recovery/MAIN_AGENT_ASSERTS/node-info 模板本体; 无 LEVEL_A; LB-1/finalize_risk/gate 逻辑明确排除未碰. ② 4 模块 version 统一 5.36.0->5.38.0 MINOR. ③ 不碰框架 Python (acp_llm/runner/agent.py/soulacp); python_tools 不改逻辑 (ENG-STDIN 仅改 .aisop.json 措辞). ④ EvolveStep + ReviewPresent 两门真停 HALT (EvolveStep user reply 'all' accepted B1-B5; ReviewPresent zero-delta finalize_risk user reply 'ok' accepted finalize; 无预授权/无 auto-approve). ⑥ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine 无 AIAP_cn.md) + .evolution_snapshot/v5.38.0/ via tool_dirs/snapshot_build.py (built LAST, snapshot_audit exit 0 mandatory). ⑦ Honest annotation: 纯一致性/卫生/文档/纪律措辞, 零执行/治理逻辑改动; B1 sections_rendered 锚定 + B5 STDIN 规则仍 prompt 级 (真保真/防卡需框架或工具侧拦截, backlog). ModifyStep 真逐节点派发已验 (ModifyStep.json, agent_id=auto-1552a7a7, tool_calls=33>0, PASS); 全 18 实现节点真 sub-agent, 0 integrity 违规. ThreeDimTest weighted=4.949 Grade S (authoritative trajectory baseline; full scope 4 modules; C=5.00 I=4.92 D=4.95). Zero-delta vs v5.36.0 (4.949), expected for 一致性/文档/纪律 alignment (per-dimension like-for-like deferred-excluded delta 0.000). 0 breaking changes, all additive. SUPERSEDES the v5.36.0 entry: Creator Evolve pipeline v5.36.0 FINALIZED by ReviewFinalize (cache/107, 2026-06-12): v5.35.0 -> v5.36.0 MINOR. ENGINE SELF-EVOLUTION (target=runner=live). Feature: three node-info template alignment (A/B/C) + FAST ENTRY entry-discipline (D). A: node_engine step4 SECTION 4 canonical Governance sub-line ('all execution results under human audit and governance', removed synonym-repeat parens). B: 4 engine-self Axiom Governance blocks (main Router.match/cache-setup/execution-summary + node_engine programExec.dispatch-plan) reordered to canonical Next/Assert/Output/Governance/Align Axiom 0 + new Output sub-line (ALL 7 sections VERBATIM) + Assert simplified + canonical Governance. C: agent_engine node_info_render contract sections corrected to 7-entry English (separator/Node State/For Audit/Report/Explain/Axiom Governance/End Separator). D: main match.step1 prepend FAST ENTRY execution-discipline preamble (go straight into match->execute->programExec, no upfront pre-analysis of target/docs; per-node reads happen at programExec step1 when needed). Honest: A/B/C are cosmetic prompt-level template-consistency; D is prompt-level entry discipline (reduces pre-analysis token waste, not 100% enforced); none change execution/governance logic. ThreeDimTest weighted=4.949 Grade S (C=5.00 I=4.92 D=4.95), zero-delta vs v5.35.0 (expected for cosmetic+discipline alignment). 4 modules unified at 5.36.0. 0 breaking changes, all additive. SUPERSEDES the v5.35.0 entry below: Creator Evolve pipeline v5.35.0 FINALIZED by ReviewFinalize (cache/104, 2026-06-11): v5.34.0 -> v5.35.0 MINOR. ENGINE SELF-EVOLUTION (target=runner=live). Feature: dispatched-node node-info fidelity — node_engine step4 SECTION 4 only. Change A: Assert sub-line strengthened to 'strictly PHYSICALLY READ and execute engine node steps, NEVER from memory; NEVER fabricate, NEVER skip, NEVER yield mid-pipeline; NEVER auto-approve a user gate (a gate is a hard STOP); CONTINUE per dispatch_plan to the terminal node.' Change B: new Output sub-line inserted after Assert, before Governance (Align Axiom 0 stays last) — 'STRICTLY render this node-info as ALL 7 sections VERBATIM; NEVER compress, merge, abbreviate, or drop a section.' Change C: OUTPUT FIDELITY hard rule in the SIX-SECTION template region — render from PHYSICALLY-READ template, never from memory; compression/omission = NODE_SUMMARY_OMITTED sovereignty-class violation. FIX-1 at finalize: SECTION 4 preamble count '4 mandatory sub-lines' -> '5'. main/agent/normal engines bumped to 5.35.0 (version field only, zero logic change). Recomputed canonical hash 01238d17 (prior v5.34.0 5c336ca7). ThreeDimTest weighted=4.949 Grade S (C=5.00 I=4.92 D=4.95). Honest: A/B render lines + C rule are prompt-level (weak/firm floors, not machine-enforced); durable fix needs framework render-verification (backlog). 0 breaking changes, all additive."
  changes_v4_0_0: "15 items: A1(AIAP.md) + A2(quality_baseline) + B1(crash recovery) + B2(circuit breaker) + B3(parallel dispatch) + B4(execution metrics) + B5(tool annotations) + C1-C8(atomic writes, cache schema, retry classification, turn_type, ASSERT gates, version sync, json_schema, identity). ThreeDimTest: 4.43 weighted (Grade A). Created by Creator Evolve pipeline (cache/129)."
  changes_v5_0_0: "14 design items + 4 quality fixes: M1(WAITING_USER/MESSAGE_PENDING relay), A1(identity definition), A2(Print-it), A3(USER_MESSAGE), A4(sys.* 24-call handling), A5(AGENT_ID), A6(on_error routing), A7(steps_done/remaining), N1(NodeVerify), N1.5(parallel wave interrupt), N2(Decision Node gate), N3(sys.* bootstrap hint), NM1-NM3(normal_engine parity). QS-01(half-open circuit breaker), QS-04(trace_id), QS-05(bootstrap validation), QS-06(normal_engine resilience parity). ThreeDimTest: 4.72 weighted (Grade A). Created by Creator Create pipeline (cache/131). Based on 05-engine-aiap-v5-design.md."
  changes_v5_2_0: "Architecture refactor (non-Creator): prepare_cache.py dual-use module introduced at python_tools/prepare_cache.py — callable as Python library (agent.py fast path ~5ms) or Bash CLI (AISOP fallback ~300ms). agent.py reduced from ~450 to ~361 lines (-89). main.aisop.json execute.step2/step3 simplified — single source of truth for cache creation. 16 test cases added (TestLibraryAPI x8 + TestCLI x5 + TestEquivalence x3), 16/16 PASS. Portable across Agent frameworks supporting Bash. Architecture aligned with 'AISOP layer only creates new caches' principle. Based on 05-prepare-cache-dual-use-plan.md."

# Evolution History
evolution:
  - version: "3.1.0"
    date: "2026-04-05"
    note: "Pre-evolution baseline. 4 modules, 14 nodes. Router + dual engine architecture established."
  - version: "4.0.0"
    date: "2026-04-05"
    note: "Major evolution via Creator Evolve pipeline (cache/129). 15 items: crash recovery, circuit breaker, parallel dispatch, execution metrics, atomic writes, ASSERT gates, json_schema, identity. ThreeDimTest 4.43 (A)."
  - version: "5.0.0"
    date: "2026-04-06"
    note: "Full rebuild via Creator Create pipeline (cache/131). 14 design items from 05-engine-aiap-v5-design.md + 4 quality fixes. sys.* 24-call handling, WAITING_USER/MESSAGE_PENDING relay, NodeVerify, Decision Node gate, identity/Print-it/USER_MESSAGE, on_error routing, steps_done tracking, half-open circuit breaker, trace_id propagation, bootstrap validation. ThreeDimTest 4.72 (A)."
  - version: "5.1.0"
    date: "2026-04-06"
    note: "Minor version bump. WAITING_USER/MESSAGE_PENDING relay refinements in engineExec.step3 (M1). Version refs sync across main/node_engine/normal_engine."
  - version: "5.2.0"
    date: "2026-04-12"
    note: "prepare_cache.py dual-use module introduced at python_tools/prepare_cache.py. Single source of truth for cache creation, callable via Python library (agent.py fast path ~5ms) or Bash CLI (AISOP fallback ~300ms). main.aisop.json execute.step2/step3 simplified — removed redundant AISOP-side cache creation logic, step3 now only updates _index.json with match result. agent.py reduced from ~450 to ~361 lines. Portable across Agent frameworks supporting Bash (Claude Code, Gemini CLI, OpenCode). Architecture aligned with 'AISOP layer only creates new caches' principle (agent.py:27-28 comment). Test suite: 16 cases (Library API + CLI + Equivalence). Based on 05-prepare-cache-dual-use-plan.md."
  - version: "5.2.1"
    date: "2026-04-12"
    note: "cleanup_cache migration — completing cache lifecycle consolidation. Moved _cleanup_cache from agent.py (55 lines) to prepare_cache.py as library-only function. prepare_cache.py now owns FULL cache lifecycle: prepare_execution_context (create) + cleanup_cache (delete). agent.py reduced from 361 to 310 lines (-51 lines). Shared helpers (_load_or_init_ctx, _atomic_write_json, _CACHE_DIR_RE) eliminate duplication. cleanup_cache preserves non-numbered dirs (legacy hex IDs). Test suite: 16 -> 20 cases (TestCleanup class with 4 new tests, all PASS). No behavior change, pure architecture consolidation. Any Agent framework can now import prepare_cache for complete cache lifecycle support."
  - version: "5.2.2"
    date: "2026-04-12"
    note: "cleanup embedded in prepare — atomic cache lifecycle. prepare_execution_context now runs cleanup_cache internally at function start (equivalent to original agent.py sequence of cleanup() then prepare(), merged into one atomic call). agent.py further simplified: removed explicit cleanup_cache import + call, removed try/except graceful-degradation fallback. agent.py reduced from 311 to 295 lines (-16). Cumulative agent.py reduction v5.1.0 -> v5.2.2: 450 -> 295 lines (-155 / -35%). cleanup_cache remains public API for explicit use (tests, maintenance). subprocess (Bash CLI fallback) automatically benefits — every prepare call ensures cleanup first. Zero API breakage, 20/20 tests still PASS."
  - version: "5.2.3"
    date: "2026-04-13"
    note: "Security + consistency hardening via Creator Evolve pipeline (cache/120). P1 AIAP.md frontmatter: tool_dirs=[], executor_shim=true (Pattern G clarification — python_tools/ is executor-internal shim, NOT registered tool), shell=false permission. P2-P4 main.aisop.json execute.step2: Bash fallback shell-echo replaced with --payload-file tempfile transport (safer per I15); execute.constraints extended with SHELL SAFETY (I15) + INPUT GUARDS (I2 5-pillar: TYPE/INJECTION/SIZE/PATH/ENCODING) + THREAT MITIGATIONS (I8 AT4/AT6). P5 normal_engine.aisop.json: circuit_breaker_state enum 2-state → 3-state (closed/open/half-open) mirroring node_engine; programExec.step3 rewritten with half-open transition (half_open_timeout_seconds=30, half_open_max_calls=5, recovery_timeout=120 for critical paths). P6 ASSERT gates (main engineExec.step1 + node_engine programExec.step1 + normal_engine programExec.step1 + agent_engine execute.step1): property predicates appended — regex match on entry_path, file_exists check, loading_mode enum, realpath workspace containment. P7 advisory only. Version bumps: main 5.2.2→5.2.3, normal_engine 5.1.0→5.2.0 (semantic circuit breaker change). All 4 .aisop.json files JSON-validated post-edit."
  - version: "5.3.0"
    date: "2026-05-13"
    note: "Observability-first evolution via Creator Evolve pipeline. 8 items: A1 OpenTelemetry SDK integration (opentelemetry-sdk capability dependency across all 4 modules, trace_id/span_id wired to OTel context propagation per gen_ai semantic conventions), A2 events[] declaration + SpanEvent dispatcher (5 events in node_engine: node_started, node_completed, node_failed, circuit_breaker_tripped, decision_gate_routed), B1 OWASP ASI threat mapping (ASI01-ASI10, 4 YELLOW items addressed: ASI01 goal_hash drift, ASI06 cache HMAC, ASI07 IPC signatures, ASI10 runtime drift), C1 version sync (all modules to 5.3.0), C2 ASSERT gate property predicate refresh, C3 normal_engine orphan function cleanup (MF31), C4 python_tools/ PL26 snapshot scope inclusion, C5 Error->on_error dual-key transitional support. All additive, zero breaking changes."
  - version: "5.4.0"
    date: "2026-05-18"
    note: "Governance + integrity hardening. 8 items (4B + 4C): B1 agent_id naming, B2 score_integrity_rules, B3 dispatch_audit, B4 score_confidence, C1 governance_hash canonical, C2 version_history append-only, C3 module_scores stale, C4+C5 version sync."
  - version: "5.4.1"
    date: "2026-05-18"
    note: "Agent_id strict enum + dispatch_audit mandatory + governance_hash TRI-SYNC. 6 items (3A + 3C): A1 agent_id strict 5-category enum (auto-<hex8>/task-<hex8>/gemini-<hex8>/inline_planned/inline_fallback) enforced in json_schema + dispatch + writeCache + python_tools, A2 dispatch_audit MANDATORY gate (CRITICAL blocks pipeline), A3 governance_hash TRI-SYNC (AIAP.md + agent_card.json + quality_baseline.json + hash_algorithm_version=v1.0), C1 version bump, C2 name sync, C3 evolution_history + bootstrap_advisory. All additive, zero breaking changes."
  - version: "5.4.2"
    date: "2026-05-19"
    note: "Internal consistency patch. 8 items (4A + 4C): A1 dispatch_audit total_nodes off-by-one fix (explicit category sum, invariant cross-validation), A2 governance_hash intermediate snapshot pipeline_note (intermediate hashes marked stale, expires_after=ReviewFinalize), A3 engine_version _ENGINE_VERSION sync 5.2.2->5.4.2, A4 initial_user_message write-once field in _index.json. C1 version bump, C2 name sync, C3 evolution_history append, C4 governance_hash deferred to ReviewFinalize. All additive, zero breaking changes."
  - version: "5.4.3"
    date: "2026-05-19"
    note: "Dispatch plan compliance patch. 8 items (4A + 4C): A1 RESUME/boot path explicit re-dispatch with dispatch_plan compliance enforcement (main engineExec.step3, node_engine RESUME_MODE_DISPATCH, agent_engine writeCache plan-violation hard validator), A2 dispatch_audit plan-violation category with cross-check against dispatch_plan from _index.json, A3 dispatch_plan_expected cache schema field (agent_engine + normal_engine + agent_write_node_cache.py auto-fill), A4 task-hex capability verification (node_engine + agent_engine capability cross-check). C1 version bump, C2 name sync, C3 evolution_history append, C4 governance_hash deferred to ReviewFinalize. All additive, zero breaking changes."
  - version: "5.4.4"
    date: "2026-05-19"
    note: "Agent_id integrity patch. 6 items (3A + 3C): A1 agent_id entropy + uniqueness HARD enforcement (unique hex chars >= 4, cross-cache uniqueness rejection, os.urandom(4).hex() mandated in resolve_agent_id), A2 dispatch_audit fake_hex 4-category detection (trivial/invalid/duplicate/low_entropy) + spawn_audit_trail timing verification (60s threshold), A3 spawn_audit_trail schema field (spawned_at, spawn_tool, parent_trace_id, hex_entropy_source) for real sub-agent evidence. C1 version bump, C2 name sync, C3 evolution_history append. All additive, zero breaking changes."
  - version: "5.5.0"
    date: "2026-05-19"
    note: "BREAKING:agent_id_self_report_removed. MAJOR evolution v5.4.4 -> v5.5.0. 9 items (4A + 5C). Root cause fix for cycle 15-17 cat-and-mouse evasion: host AI loses self-report agent_id capability. A1 agent_id server-side generation via NEW agent_id_generator.py (secrets.token_hex(4), PRNG entropy controlled by Engine, written to _index.json::dispatch_plan[node].expected_agent_id before dispatch). A2 --agent_id CLI param removed from agent_write_node_cache.py (auto-reads from _index.json, deprecated flag ignored). A3 dispatch_audit.py simplified to integrity-only checking (removed fake_hex_trivial/invalid/duplicate/low_entropy + suspicious_sub_agent, single integrity_violation category: cache.agent_id == expected_agent_id). A4 spawn_audit_trail timestamp anti-forgery via dispatch_records (generated_at written by agent_id_generator.py, spawned_at > generated_at ordering invariant verified by dispatch_audit). C1 version bump, C2 name sync, C3 evolution_history append (BREAKING marker), C4 governance_hash TRI-SYNC recompute, C5 agent_engine json_schema update. BREAKING: not backward compatible with v5.4.x self-report agent_id cache schema. ThreeDimTest: C=4.714 I=4.923 D=4.850 weighted=4.849 (Grade S)."
  - version: "5.6.0"
    date: "2026-05-20"
    note: "Fallback tightening with spawn_failure_evidence. MINOR evolution v5.5.0 -> v5.6.0. 8 items (4A + 4C). Evidence: cycle 19 ObservabilityStep used inline_fallback with vague fallback_reason — cannot distinguish real spawn failure from never-attempted spawn. A1 spawn_failure_evidence schema REQUIRED for inline_fallback (attempted_spawn_tool, attempted_at > generated_at, failure_signal enum, failure_detail structured). A2 no-evidence fallback = FAIL with SPAWN_NOT_ATTEMPTED. A3 dispatch_audit.py restored legit/illegitimate fallback classification. A4 agent_id_generator.py prefix alignment with capability_probe.recommended. C1 version bump, C2 name sync, C3 evolution_history append, C4 governance_hash TRI-SYNC. 0 breaking changes."
  - version: "5.6.1"
    date: "2026-05-26"
    note: "Reliability patch via Creator Evolve pipeline (cache/32). PATCH evolution v5.6.0 -> v5.6.1. 3 items (1A + 1B + 1C-implicit): A2 transient cache retry for node_engine (retry with exponential backoff on transient file system errors during cache read/write), A3 degraded_reason enum (structured enum replacing free-text degraded_reason in NodeVerify: TOOL_CALL_DEFICIT, PARTIAL_OUTPUT, TIMEOUT, TRANSIENT_FAILURE), B1 writeCache read-back verification in agent_engine (atomic write followed by read-back comparison to detect silent write corruption). 0 breaking changes. ThreeDimTest: C=4.857 I=5.000 D=4.700 weighted=4.874 (Grade S)."
  - version: "5.9.0"
    date: "2026-05-27"
    note: "Dispatch plan generation vulnerability fix via Creator Evolve pipeline (cache/40). MINOR evolution v5.6.1 -> v5.7.0. 3 A-level + 1 B-level + C-sync items. A1 NEW python_tools/generate_dispatch_plan.py deterministic tool. A2 node_engine.aisop.json programExec.step2 rewritten. A3 dispatch_audit.py --pre-execution generation-stage gate. B1 version bump. 0 breaking changes. ThreeDimTest 4.874 (Grade S)."
  - version: "5.10.0"
    date: "2026-05-28"
    note: "HMAC Phase 2 + NIST CAISI + compliance hardening via Creator Evolve pipeline (cache/45). MINOR evolution v5.9.0 -> v5.10.0. A1 HMAC Phase 2 runtime active (signing/verification when key present). B1 NIST CAISI three-pillar alignment propagated to all engines. B2 HMAC delegation contract formalized. B3 EU CRA SRP registration readiness. B4 Art.50 CoP tracking. B5 OTel Logs API dual-emit preparation. C1-C3 governance sync + summary trimming. 0 breaking changes."
  - version: "5.12.0"
    date: "2026-05-29"
    note: "User_gate enforcement remediation via Creator Evolve pipeline (cache/47). MINOR evolution v5.11.0 -> v5.12.0 FINALIZED. G1 auto-detect gates from node definition text (user_gate_audit.py enhancement). G3 multi-file node parsing for gate detection across delegated modules. G4 effective coerce to FAIL+halt (not WAITING_USER) when sovereignty bypass detected. HMAC_CONSTANT_TIME MUST enforcement upgraded from advisory per CVE-2026-21713. HMAC key rotation 4-step procedure documented. MCP 2026 roadmap tracking (Server Cards, Streamable HTTP, DPoP, Tasks). B3 sovereignty gate precedence retained. ThreeDimTest: C=4.929 I=4.923 D=5.000 weighted=4.947 Grade S. 0 breaking changes."
  - version: "5.13.0"
    date: "2026-05-29"
    note: "nodes_in_path completeness + NODE SUMMARY user language via Creator Evolve pipeline (cache/48). MINOR evolution v5.12.0 -> v5.13.0 FINALIZED. A1 dispatch_audit.py nodes_in_path completeness verification (independent mermaid reparse, QI-2, gate omission CRITICAL, decision branch exempt). A2 node_engine programExec step2 wire completeness check. B1 NODE SUMMARY forced user language. C1-C5 version sync, name sync, evolution_history, governance_hash TRI-SYNC, summary. 0 breaking changes. ThreeDimTest: C=5.000 I=4.923 D=4.950 weighted=4.950 Grade S."
  - version: "5.13.1"
    date: "2026-05-29"
    note: "Branch-aware completeness fix via Creator Evolve pipeline (cache/49). PATCH evolution v5.13.0 -> v5.13.1. A1 dispatch_audit.py branch-aware completeness verification (walked branches checked, unwalked branches exempt, edge label regex fix — prevents false node matches from Pass/Fail/Red/Yellow edge labels). A2 node_engine step2 wiring recalibrated for branch-aware semantics. C-auto version bump, name sync, governance_hash TRI-SYNC deferred to ReviewFinalize. 0 breaking changes."
  - version: "5.13.2"
    date: "2026-05-29"
    note: "Python subprocess prefix alignment via Creator Evolve pipeline (cache/50) FINALIZED. PATCH evolution v5.13.1 -> v5.13.2. C-PREFIX-1/C-PREFIX-2: agent_engine writeCache step2/step3 python subprocess call prefix aligned from bare 'python' to '<python> -X utf8', consistent with node_engine/normal_engine convention (prevents Windows non-ASCII encoding issues per PEP 686). C-auto version bump, name sync, evolution_history append, governance_hash TRI-SYNC. ThreeDimTest: C=4.930 I=4.960 D=5.000 weighted=4.960 Grade S. 0 breaking changes."
  - version: "5.14.0"
    date: "2026-05-29"
    note: "X3 sovereignty enforcement documentation formalization via Creator Evolve pipeline (cache/51) FINALIZED. MINOR evolution v5.13.2 -> v5.14.0. C1: Fixed agent_engine description — G4 FAIL+halt narrative corrected to X3 WAITING_USER rewrite via user_gate_audit.py --enforce (FLAW-A resets _index, FLAW-B reconstructs steps_done). C2: Updated node_engine e2f label to X3 semantics. C3: Replaced 'pending v5.14.0 bump' version placeholders. C4: Updated summary fields across all 4 modules. C5: Version bump 5.13.2 -> 5.14.0 + name sync. C8: Industry research gap report — Layer-1 vs Layer-2 enforcement comparison (4 gaps, 4 backlog items). X3 execution logic was manually implemented between v5.13.1->v5.13.2; this version formalizes documentation. No execution body changes — step3 --enforce, e2f --enforce, FLAW-A/B behavior, NON-SKIPPABLE all preserved verbatim. ThreeDimTest: C=4.900 I=5.000 D=5.000 weighted=4.975 Grade S. 0 breaking changes."
  - version: "5.14.1"
    date: "2026-05-29"
    note: "PATCH evolution v5.14.0 -> v5.14.1 via Creator Evolve pipeline (cache/52) FINALIZED. 3 bug fixes + 5 C-auto items. A1 node_engine programExec step5 _index.json merge via agent_update_index.py (prevents loss of user_gates_presented during concurrent writes). A2 node_engine programExec step4 language fallback chain changed to conversation language detection before English fallback. A3 agent_engine init step6 RESUME_MODE completion mandate — after RESUME_MODE execution, full review->writeCache pipeline MUST execute including step2 agent_update_index.py and step3 user_gate_audit.py --enforce (prevents bypass of nodes_status update and sovereignty enforcement on resumed nodes). Guardrails verified: writeCache.step3 and X3 sovereignty enforcement NOT MODIFIED. ThreeDimTest: C=4.930 I=5.000 D=4.950 weighted=4.97 Grade S. 0 breaking changes."
  - version: "5.14.2"
    date: "2026-05-30"
    note: "PATCH (documentation only) v5.14.1 -> v5.14.2 via Creator Evolve pipeline (cache/53) FINALIZED. A1: added 'Known Limitations (v5.14.2)' section to AIAP.md documenting underlying-runtime (Claude Code/Node/V8) long-session JS heap OOM — symptom: single long-running node session accumulation -> child process exit 0xC0000409 (abort/heap OOM), crash node drifts; root cause: upstream Claude Code not-planned (GitHub #25926 mutableMessages monotonic growth never released + #11155 tool-output whole-session residency), explicitly NOT an engine defect; diagnosis: child NODE_OPTIONS += --heapsnapshot-near-heap-limit=2 --trace-gc to capture pre-crash heap; mitigation: --max-old-space-size headroom + process boundary (existing soulacp retry / proactive session rotation / heavy-node segmentation as root-fix), with note that /compact and sub-agent do NOT solve (same process). C1-C4 auto: version bump 5.14.1->5.14.2 across 4 .aisop.json + AIAP.md frontmatter + agent_card.json + quality_baseline.json, name/summary/description current-version sync (historical changelog lineage preserved), evolution_history append, governance_hash TRI-SYNC recompute. Doc-only invariant machine-verified: node/agent/normal engine function bodies byte-identical to v5.14.1 snapshot (ZERO execution-logic edits); X3 writeCache.step3 --enforce, node_engine step5 _index merge (agent_update_index.py / user_gates_presented / last_completed), e2f / FLAW-A/B / WAITING_USER / NON-SKIPPABLE all preserved verbatim; main.execute.step3 prose corrected to match existing prepare_cache.py _resolve_engine_version runtime behavior (doc-to-code alignment, no logic change). ThreeDimTest (Generate2 full-scope fresh retest): C=4.5 I=4.577 D=4.5 weighted=4.531; final_adjusted post-Finalize D6/D10 reconciliation (version_sync 4.5->5.0, description 4.5->5.0): D_avg 4.5->4.8, final_adjusted=4.621 Grade S. Stage weighted-delta vs v5.14.1 (4.97) is a fresh-retest scoring-method artifact (deferred-neutral 4.5 per P23 + full re-score), NOT an execution-path regression (0 path-coverage drop, 0 dead nodes, 0 changed execution steps). 0 breaking changes."
  - version: "5.15.0"
    date: "2026-05-30"
    note: "MINOR evolution v5.14.2 -> v5.15.0 via Creator Evolve pipeline (cache/57) — DRAFT generated by Generate1 (finalized by ReviewFinalize). B1 (LEVEL_B, user-authorized turn 57): agent-mode last_completed_node advancement. ROOT CAUSE (empirically verified — live _index of this run had last_completed_node stuck at ProtocolAlign while the pipeline was at EvolveStep): agent-mode direct-write (v5.6.1) bypasses the last_completed auto-advance that agent_write_node_cache.py performs only for inline nodes; agent_update_index.py is a pure merge with no auto-advance, so an agent-heavy pipeline (creator: 18 agent + 2 inline) leaves _index.last_completed_node stuck at the last inline node for the entire run (crash-recovery still works via current_node + nodes_status). FIX (single change, agent_engine.writeCache.step2 only): when the node is terminal/completed (status in PASS/FAIL/PARTIAL/DEGRADED/ABORTED; EXCLUDE pause states WAITING_USER/MESSAGE_PENDING), merge last_completed_node={YOUR_NODE} into the EXISTING agent_update_index.py --updates call alongside nodes_status (same single write, NO new tool call). X3 PAIRING (no code change): user_gate_audit.py FLAW-A un-advance branch — previously dead for agent gate nodes because last_completed never advanced — now activates; on gate bypass with last_completed_node==forced node, FLAW-A un-advances to the prior node automatically. This cycle EXPLICITLY AUTHORIZES writing _index.last_completed_node (reversing the prior doc-only-PATCH 'do not touch last_completed' constraint); presented + confirmed at EvolveStep sovereignty gate (user reply 'all', Axiom 0 satisfied). C-auto: C1 version bump 5.14.2->5.15.0 across 4 .aisop.json + AIAP.md frontmatter + agent_card.json + quality_baseline.json (protocol_config.json not present — skipped); C2 name field version sync (4 modules); C3 evolution_history + changelog append (this entry); C4 governance_hash recompute + TRI-SYNC at ReviewFinalize; C5 AIAP.md parallel-wave concurrent last_completed_node single-value caveat documented (documentation only, no concurrency-model change). HARD CONSTRAINTS (preserved): (1) ONLY agent_engine.writeCache.step2 changed — no node_engine / normal_engine step change, no X3 writeCache.step3 / node_engine e2f --enforce change, no user_gate_audit.py / agent_update_index.py CODE change. (2) Parallel-wave concurrency: caveat documented only, concurrency model unchanged. (3) Version per evolution_rules LEVEL_C auto. Backward compatible (additive single _index key; old caches readable). 0 breaking changes. NOTE: scores below are pending ReviewFinalize re-measure."

  - version: "5.16.0"
    date: "2026-05-30"
    note: "MINOR evolution v5.15.0 -> v5.16.0 via Creator Evolve pipeline (cache/58) — DRAFT generated by Generate1 (finalized by ReviewFinalize). B1 (LEVEL_B, user-authorized): normal_engine sovereignty gate enforcement. user_gate_audit.py --enforce added to normal_engine writeCache.step3, matching agent_engine writeCache.step3 and node_engine programExec.step3.e2f sovereignty enforcement pattern. Programs with loading_mode=normal now have Axiom 0 Layer-1 backstop parity. C-auto: C1 agent_engine writeCache field count harmonized (step1 says 15, step1_5 now also says 15), C2 main.aisop.json system_prompt placeholder '{system_prompt}' replaced with actual content, C3 normal_engine Error fields aligned to error_taxonomy categories, C4 agent_engine description trimmed to current-version-only (changelog history moved to AIAP.md), C5 AIAP.md benchmark note updated from v5.14.1 to v5.15.0 reference, C6 version sync across all 4 modules + governance files, C7 governance hash intermediate recompute. 0 breaking changes."

  - version: "5.17.0"
    date: "2026-05-31"
    note: "MINOR evolution v5.16.0 -> v5.17.0 via Creator Evolve pipeline (cache/62) — DRAFT generated by Generate1. Sovereignty Bug Protocol. A1 sovereignty_bug_protocol shared section (main.aisop.json + agent_engine.aisop.json). B1 RULES.sovereignty_bug execution rule in agent_engine. C1-C6 version bump + metadata sync. Reference: plan20/34_permissive_sovereignty_gate_framework.md. 0 breaking changes."

  - version: "5.18.0"
    date: "2026-06-03"
    note: "MINOR evolution v5.17.0 -> v5.18.0 via Creator Evolve pipeline (cache/68) FINALIZED by ReviewFinalize. User Language Anchor — structured user_language anchoring replacing per-node ad-hoc detection (continues v5.13.0 language handling initiative). B1 (LEVEL_B) prepare_cache.py _detect_user_language() with ja/ko detection + persistent user_language field in _index.json. B2 (LEVEL_B) node_engine USER_LANGUAGE bootstrap param passed to sub-agents. B3 (LEVEL_B) agent_engine + normal_engine USER_LANGUAGE param + enforcement constraint. C1 version bump 5.17.0 -> 5.18.0 all 4 modules + governance files. C2 name field version sync. C3 evolution_history + changelog append. ThreeDimTest partial: C=5.00 I=5.00 D=4.60 weighted=4.880 Grade S. Post-Finalize projected: 5.000. 0 breaking changes."

  - version: "5.19.0"
    date: "2026-06-04"
    note: "MINOR evolution v5.18.0 -> v5.19.0 via Creator Evolve pipeline (cache/72) FINALIZED by ReviewFinalize. B1 (LEVEL_B) node_engine step4 EXPLAIN forced per-summary [PHYSICAL READ] user_language anchor -- prevents orchestrator language drift in long pipelines. C1-C4 version/name/history/hash sync. ThreeDimTest: C=5.00 I=4.96 D=5.00 weighted=4.982 Grade S. Direction continuity 0.95 HIGH (continues v5.18.0 User Language Anchor). 0 breaking changes."

  - version: "5.20.0"
    date: "2026-06-04"
    note: "MINOR evolution v5.19.0 -> v5.20.0 via Creator Evolve pipeline (cache/73) FINALIZED by ReviewFinalize. B1 (LEVEL_B): main.aisop.json engineExec step key renaming — non-standard keys (step1_integrity, step1_lite) renamed to sequential format (step2, step3) with downstream step numbers shifted. Fixes STEP_KEY_FORMAT YELLOW. B2 (LEVEL_B): normal_engine.aisop.json inlineEnd Error field addition — terminal node now has error handling (retry->circuit-breaker->fallback->inform). Fixes INLINEEND_ERROR YELLOW. C1-C4 version/name/history/hash sync. ThreeDimTest: C=5.000 I=4.962 D=5.000 weighted=4.983 Grade S. D6 reconciled 4.5->5.0 post-Finalize. governance_hash TRI-SYNC sha256:76667e06. 0 breaking changes."

  - version: "5.21.0"
    date: "2026-06-05"
    note: "MINOR evolution v5.20.0 -> v5.21.0 via Creator Evolve pipeline (cache/76) FINALIZED by ReviewFinalize. B1 (LEVEL_B core root cause fix): prepare_cache.py _strip_non_natural_language() — language detection now strips file paths, snake_case identifiers, URLs, version numbers, hex hashes before CJK/ASCII ratio analysis. Fixes _detect_user_language path/ASCII interference (highest-frequency source bug: evolution commands with long paths pollute character ratio). User-provided cache/75 evidence validated: CJK ratio 36.89%->43.84%, correct 'zh' detection. B2 (LEVEL_B safety net): USER_LANGUAGE_CONFLICT_DETECTION added to agent_engine/normal_engine/node_engine execute.constraints — cross-checks _index.user_language vs user_message natural language, prefers user_message on conflict + emits structured warning. C1-C4 version/name/history/hash sync. Direction continuity 0.92 HIGH (4-version user_language accuracy trajectory: v5.18.0->v5.19.0->v5.20.0->v5.21.0). ThreeDimTest: C=5.000 I=5.000 D=5.000 weighted=5.000 Grade S. D6/D10 reconciled 4.5->5.0 post-Finalize. Simulation: 22/22 PASS 100% coverage. NihilDensity: GREEN 0.00681. governance_hash TRI-SYNC sha256:f75cfaf6. 0 breaking changes."

  - version: "5.23.0"
    date: "2026-06-05"
    note: "MINOR evolution v5.21.0 -> v5.23.0 via Creator Evolve pipeline (cache/80) FINALIZED by ReviewFinalize. 4 B-level items + C-auto. B1 (LEVEL_B): prepare_cache.py CJK body heuristic — absolute threshold (>= 5 CJK ideograph chars) replaces ratio-based check for _detect_user_language. Prevents false en detection when CJK message contains heavy English technical terms. B2 (LEVEL_B): node_engine 3-section Node Summary template — fixed 3-section format (Node State + Summary + Explain). Summary pure-forwards user_summary VERBATIM as stable linguistic base (context-window-decay prevention). Explain provides orchestrator cross-node analysis in USER_LANGUAGE. B3 (LEVEL_B, sovereignty toolset): user_gate tri-state mechanism — user_gate_audit.py _extract_user_gate supports true/false/conditional (previously only true+text_marker). Eliminates systemic false-positive coerce of ValidateStep (false) and ReviewPresent (conditional). EvolveStep (true) sovereignty enforcement intact. B4 (LEVEL_B): agent_engine USER_SUMMARY RENDERING CONTRACT strengthened — user_summary feeds 3-section Node Summary template verbatim. Contract extended to ALL node types (inline, technical, delegation wrapper). Direction continuity HIGH (continues v5.18.0-v5.21.0 language-drift root-cause fix trajectory). ThreeDimTest: C=5.000 I=5.000 D=4.850 stage-weighted=4.955 Grade S. D6/D10 reconciled post-Finalize. Simulation: 24/24 PASS 100% coverage. governance_hash TRI-SYNC sha256:88a590c8. 0 breaking changes."

  - version: "5.24.0"
    date: "2026-06-09"
    note: "MINOR evolution v5.23.0 -> v5.24.0 via Creator Evolve pipeline (cache/85). 4 Bug fixes + 3 Fix items + 2 A-level items + C-auto. Bug1A-D: node_engine 3-section Node Summary template extended to ALL node types (inline + bootstrap, no exemptions) with mode_detail (agent:{agent_id}/inline) and mandatory newline separator. FixA1: RESUME RE-ENTRY ENFORCEMENT — resume after crash re-enters full step3(b)->(d) dispatch sequence for agent nodes. FixA2: POST-WAITING_USER re-dispatch with fresh agent_id_generator + dispatch_records sync. FixB: DISPATCH INTEGRITY CHECK 4-criterion positive evidence (filename, execute_mode+trace, spawn timestamp, agent_id match) with DISHONEST_DOWNGRADE. A1: agent_engine writeCache.step1(b) ordered JSON skeleton template with 16 fields (prevents sub-agent field omission). A2: EXPECTED_AGENT_ID in node_engine bootstrap template + agent_engine params. C1-C4 version/name/history/hash sync. Direction continuity HIGH (execution fidelity fixes v5.4.3-v5.24.0, 8th consecutive cycle). 0 breaking changes. ThreeDimTest: C=5.00 I=5.00 D=5.00 weighted=5.000 Grade S."

  - version: "5.26.0"
    date: "2026-06-09"
    note: "MINOR evolution v5.24.0 -> v5.25.0 via Creator Evolve pipeline (cache/87) FINALIZED by ReviewFinalize. 3 B-level items + C-auto. B1 [Fix A''] node_engine programExec.step3(f) RESUME/CRASH RE-ENTRY expanded from step3(b)->(d) to full (a)->(h) cycle including step4 three-section template output. B2 [P4] node_engine programExec.step3(e2d) user_gates_presented audit schema expanded with user_answer/accepted/rejected + second append-write on resume. B3 [Fix C] main engineExec ExecutionSummary USER_LANGUAGE [PHYSICAL READ] anchor added before rendering. C1-C4 version/name/history/hash sync. Direction continuity HIGH (resume completeness v5.6.1->v5.14.1->v5.24.0->v5.25.0, USER_LANGUAGE v5.18.0->v5.21.0->v5.25.0, audit trail v5.4.0->v5.12.0->v5.25.0). ThreeDimTest: C=4.893 I=4.714 D=4.900 weighted=4.814 Grade S. Simulation: 46/46 PASS 100% coverage. Validation: 67P/14Y/1R. NihilDensity: GREEN 0.17%. governance_hash TRI-SYNC sha256:a3affdf7. 0 breaking changes."

  - version: "5.27.0"
    date: "2026-06-09"
    note: "MINOR evolution v5.26.0 -> v5.27.0 via Creator Evolve pipeline (cache/90) FINALIZED by ReviewFinalize. 8 items + C-auto. #1b blank line (paragraph break) before separator line in node_engine step4. #4a Summary->Report rename+thicken — Section 2 VERBATIM FORWARD with thin-content reconstruction. #4b Explain non-authoritative disclaimer — Section 3 context-decay-prone NON-AUTHORITATIVE. #4c User Message complete forwarding — every user_message forwarded in full. #4d agent_engine writeCache user_summary quality rule (min 2-3 substantive sentences). #5-scope USER_LANGUAGE expanded to all node-info prose/labels (not just Explain). #5-axiom Axiom 0 fixed canonical English (DO NOT TRANSLATE). §9 context-compaction re-entry protocol (PHYSICAL READ + full (a)->(h) cycle). C1-C4 version/name/history/hash sync. Direction continuity HIGH (node-info template v5.23.0->v5.24.0->v5.26.0->v5.27.0, USER_LANGUAGE v5.18.0->v5.21.0->v5.27.0). ThreeDimTest: C=5.0 I=4.9 D=4.8 weighted=4.90 Grade S. 0 breaking changes."

  - version: "5.30.0"
    date: "2026-06-10"
    note: "MINOR evolution v5.29.0 -> v5.30.0 via Creator Evolve pipeline (cache/96) — DRAFT generated by Generate1 (finalized by ReviewFinalize). HYGIENE RE-BASELINE: formalized from manual hotfix 2026-06-10 (plan22/03 §15/§18, plan22/06). This cycle registers + unifies version only; the 5 hotfix logic bodies are ALREADY LIVE in files (verified present at Research1/EvolveStep, NOT re-modified per hard-constraint①). 5 formalized hotfixes: (1) Bug① agent_id_generator.py --resume + node_engine resume wiring (live evidence: cache/94 superseded_agent_ids + resume_regenerated). (2) Bug②/E5 agent_update_index.py recursive _deep_merge + dotted-path _unflatten. (3) Bug③ agent_engine.aisop.json writeCache STRING ESCAPING requirement (json.dumps semantics for inner quotes/backslashes/newlines). (4) E3 user_gate_audit.py auto_approve fingerprint gate. (5) E4 agent_update_index.py presented_at backfill (empty/midnight). C1 version unify to 5.30.0 across all 4 .aisop.json (main/node_engine/agent_engine/normal_engine) + AIAP.md frontmatter + agent_card.json + quality_baseline.json — ELIMINATES Q2 version split (main+normal were 5.27.0, node+agent were 5.29.0). C2 name field version sync to v5.30.0 (4 modules). C3 evolution_history + changelog append (this entry). C4 governance_hash recompute (tool_dirs/governance_hash.py, absolute path) + TRI-SYNC (AIAP.md / agent_card.json / quality_baseline.json; no AIAP_cn.md) + .evolution_snapshot/v5.30.0/ via tool_dirs/snapshot_build.py --pipeline_run_id=80548c5f-0d71-410f-8b9b-8474c161ec33 (snapshot_audit MUST exit 0) — deferred to ReviewFinalize. python_tools/ hotfixes NOT in governance_hash scope and NOT modified. Authorized at EvolveStep sovereignty gate (user reply 'all', accepted C1-C4, Axiom 0 satisfied; no pre-authorization assumed). 0 breaking changes. NOTE: scores below pending ReviewFinalize re-measure."

  - version: "5.31.0"
    date: "2026-06-10"
    note: "MINOR evolution v5.30.0 -> v5.31.0 via Creator Evolve pipeline (cache/97) FINALIZED by ReviewFinalize. Two change classes: A=startup visibility (prompt-level, honestly NON-machine-enforced) + B=metadata summary hygiene. A1 main.aisop.json Router: startup MUST emit a visible decision chain — Router:[match=... -> classify=... -> route=<direct_reply|engine:loading_mode>] + Engine:[loaded v<X>] + Cache:[ready trace=...] (strengthens existing Router:/Engine:/Cache: markers, no step deleted). A2 node_engine.aisop.json programExec step1-3: MUST emit one-line pre-dispatch setup trace (Engine: target read | dispatch_plan: N nodes (modes) | audits: pre=OK / completeness=... | dispatch start) — turns setup visibility from variance to mandatory; existing generate_dispatch_plan/dispatch_audit/completeness logic NOT modified. A3 node_engine.aisop.json step4: appended after the existing Bug1-A/Bug1-B clause — startup nodes + normal-mode first-batch inline nodes (Start/NLU/PipelineEntry) ABSOLUTELY not exempt from the six-section node-info, Node State MUST carry the (inline) marker, no silent startup skip; honestly annotated prompt-level, non-machine-enforced. B1 strip stale hardcoded version prefix (e.g. '...v5.27.0:') from the summary field of all 4 modules (main/node_engine/agent_engine/normal_engine) -> version-agnostic semantic description refreshed to v5.31.0. B2 node_engine summary compressed to <500 chars (kept essentials, dropped redundant history enumeration). C1 version unify 5.30.0 -> 5.31.0 (4 .aisop.json + AIAP.md + agent_card.json + quality_baseline.json). C2 name field version sync to v5.31.0 (4 modules). C3 evolution_history + AIAP.md evolution append (this entry). C4 governance_hash recompute via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY) + TRI-SYNC (AIAP.md / agent_card.json / quality_baseline.json; no AIAP_cn.md) + .evolution_snapshot/v5.31.0/ via tool_dirs/snapshot_build.py --pipeline_run_id=00443298-8256-41ad-b12a-254d839bec3b (snapshot_audit exit 0 / meta_valid:true mandatory; never inline hash). HARD: only main Router step + 4 summaries + node_engine step1-3/step4 prompt + version touched; python_tools/dispatch_plan/hotfix NOT touched. Authorized at EvolveStep sovereignty gate (user reply 'all', accepted A1/A2/A3/B1/B2/C1-C4; Axiom 0 satisfied; no pre-authorization assumed; gate not reopened at ReviewFinalize). ThreeDimTest weighted=4.5 Grade S (this-cycle Professional C*0.25+I*0.45+D*0.30; prior full baseline 4.814 EXEMPT per ObservabilityStep method/scope-change exemption — NOT a real regression, baseline-tracking continuity preserved). Validation 0 RED, Simulation 36/36 PASS 100% coverage, NihilDensity GREEN, PostSimulateGate GREEN. 0 breaking changes, all additive."

  - version: "5.32.0"
    date: "2026-06-11"
    note: "MINOR evolution v5.31.0 -> v5.32.0 via Creator Evolve pipeline (cache/99) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target=runner=live). P0 hotfix formalization + node_engine step4 micro-refinement. Two change classes. A=formalize already-live P0 inline-node hotfixes (this cycle only registers changelog + bumps version + recomputes hash; the 4 hotfix bodies are ALREADY LIVE in files, logic NOT re-modified): A1 node_engine.aisop.json programExec.step1 MULTI-GRAPH / DELEGATION EXPANSION segment — already on disk, changelog-registered, logic unchanged. A2 node_engine.aisop.json programExec.step2 completeness BLOCK including entry_path_omissions>0 — already on disk, changelog-registered. A3 main.aisop.json execute.step1 A1 classify wording (classify=package_route, NOT intent) — already on disk, changelog-registered. A4 python_tools/dispatch_audit.py entry_path_omissions — python_tool is OUT of governance_hash scope, kept as-modified, changelog-registered. (A1-A4 were cache/98 re-run live-verified nodes_in_path=21; this cycle only formalizes + bumps version.) B=node_engine.aisop.json step4 SECTION 4 Axiom Governance Assert reword: 'Assert: strictly follow dispatch_plan; NEVER fabricate, NEVER skip' -> 'Assert: strictly execute engine node steps; NEVER fabricate, NEVER skip' (this one line only; other three SECTION 4 lines untouched). C=node_engine.aisop.json step4 new SECTION 5 End Separator: after the final SECTION 4 line 'Align Axiom 0: Human Sovereignty and Wellbeing', append an end separator symmetric with the opening SECTION 0 separator — format '━━━━━━━━━━ end [{N}/{total}] {NodeName} ━━━━━━━━━━' followed by a blank line; honestly annotated as prompt-level UI improvement, NON-machine-enforced; new SECTION 5 (End Separator), no node type exempt. B+C content edits were applied by Generate1; ReviewFinalize verified them in place (node_engine.aisop.json step4 carries the new Assert + SECTION 5). C1 version unify 5.31.0 -> 5.32.0 (4 .aisop.json + AIAP.md + agent_card.json + quality_baseline.json). C2 name field version sync to v5.32.0 (4 modules). C3 evolution_history + version_history + AIAP.md evolution append (this entry). C4 governance_hash recompute via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC (AIAP.md / agent_card.json / quality_baseline.json; no AIAP_cn.md, TRI=3) + .evolution_snapshot/v5.32.0/ via tool_dirs/snapshot_build.py --pipeline_run_id=f68ded7c-0ffd-4cf8-b18d-3bee8f0906f0 (snapshot_audit exit 0 mandatory; never inline hash). PURPOSE: eliminate the hash drift vs 3bab5655 by formally re-baselining the canonical hash to the post-hotfix file state, plus step4 task 4/6. HARD: only changelog registration + node_engine step4 two real edits (Assert + End Separator) + version/summary strings touched; python_tools/ NOT in hash scope and NOT modified; no hotfix behavior logic touched; nothing outside the engine target. Authorized at EvolveStep sovereignty gate (TRUE HALT, user reply 'all', accepted A/B/C; Axiom 0 satisfied; no pre-authorization assumed; gate NOT reopened at ReviewFinalize). ThreeDimTest weighted=4.829 Grade S (full scope, all 4 modules scored). Validation 12/12 0 RED, Simulation 19/19 PASS 100% coverage, NihilDensity GREEN max_nihil=0.0, PostSimulateGate GREEN. 0 breaking changes, all additive."

  - version: "5.33.0"
    date: "2026-06-11"
    note: "MINOR evolution v5.32.0 -> v5.33.0 via Creator Evolve pipeline (cache/100) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target=runner=live). Fix #1 score_confidence read-back off-by-one — single-clause protocol-text integrity fix. A1 agent_engine.aisop.json writeCache.step1_5 (d2) read-back validation: the text 'Verify ALL 15 fields are present (status, completed, output, tool_calls, modifications, user_summary, execute_mode, agent_id, cache_schema_version, steps_done, steps_remaining, trace_id, span_id, user_messages, l10_audit)' omitted score_confidence, while writeCache.step1's ORDERED JSON SKELETON writes ALL 16 fields (last is score_confidence). off-by-one: step1 wrote 16, step1_5 read-back verified only 15. Corrected to 'Verify ALL 16 fields are present (..., l10_audit, score_confidence)' — fully aligned with the step1 skeleton's 16 fields. Impact: score_confidence is the DEGRADED scoring field (status=DEGRADED -> score_confidence=low, ReviewFinalize caps weighted delta on it); read-back omitting it was a completeness gap. This cycle RESOLVES the long-disclosed Q1 residual (carried in v5.31.0 / v5.32.0 disclosed_residuals). step1 SKELETON (already 16) NOT touched; step1_5 other clauses NOT touched; no other step/module/target-external touched. C1 version unify 5.32.0 -> 5.33.0 (4 .aisop.json + AIAP.md + agent_card.json + quality_baseline.json). C2 name field version sync to v5.33.0 (4 modules). C3 evolution_history + version_history + AIAP.md evolution append (this entry). C4 governance_hash recompute via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC (AIAP.md / agent_card.json / quality_baseline.json; no AIAP_cn.md, TRI=3) + .evolution_snapshot/v5.33.0/ via tool_dirs/snapshot_build.py --pipeline_run_id=1e96a7f7-bd96-4804-b6bc-89f8a9987b49 (snapshot_audit exit 0 / meta_valid:true mandatory; never inline hash). Recomputed canonical hash 6c888fe4d3b3702f61a7c67fb2bff9b6ac130854cb9a89bcc77bd3eeecc07077 matches ValidateStep --verify-only precompute (cross-validated). HARD: only agent_engine writeCache.step1_5 (d2) field count (15->16) + field list (+score_confidence) edited; python_tools/ NOT in hash scope and NOT modified; nothing outside the engine target. Authorized at EvolveStep sovereignty gate (TRUE HALT, user reply 'all', accepted A1; Axiom 0 satisfied; no pre-authorization assumed; gate NOT reopened at ReviewFinalize). ThreeDimTest weighted=4.944 Grade S (full scope, all 4 modules scored; Professional C*0.25+I*0.45+D*0.30, C=4.96 I=4.92 D=4.96). Validation 14/14 0 RED, Simulation 18/18 PASS 100% coverage, NihilDensity GREEN, PostSimulateGate GREEN. 0 breaking changes, all additive."

  - version: "5.34.0"
    date: "2026-06-11"
    note: "MINOR evolution v5.33.0 -> v5.34.0 via Creator Evolve pipeline (cache/101) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target=runner=live). Feature: engine-self node-info — the executor now renders structured six-section node-info for its OWN engine steps ([engine] marker, distinct from dispatched nodes' [N/total]). A1 4 new engine-self render points: main.aisop.json (3) — execute.step1 -> '[engine] Router.match' (For Audit: package/loading_mode/route), execute.step2-4 -> '[engine] Router.cache-setup' (For Audit: cache/trace/engine-version + HMAC/governance_hash integrity), engineExec.step6-8 -> '[engine] Router.execution-summary' (For Audit: execution_metrics + dispatch_audit severity); node_engine.aisop.json (1) — programExec.step2 single-line 'MANDATORY PRE-DISPATCH SETUP TRACE (A2 v5.31.0)' UPGRADED to six-section '[engine] programExec.dispatch-plan' (For Audit: nodes_in_path/dispatch_plan agent-inline/audits pre+completeness+entry_path_omissions), emitted BEFORE the first dispatched node (P0 early-visibility point per hard-constraint③). B engine-self node-info template (six sections + [engine] {step} identity, strictly NOT mixed with [N/total]) + custom Axiom Governance Assert (PHYSICALLY READ + render full template VERBATIM each step; never from memory; never skip match->cache-setup->dispatch-plan->summary). D render_claim: each engine-self node writes _index.json::engine_self_status[{step}].node_info_render_claim {rendered, sections_rendered, user_language_used} for band-external E1 self-consistency audit, reusing agent_update_index.py (no new python tool). HARD-CONSTRAINT①: dispatched-node step4 six-section render UNCHANGED (creator node-info untouched). C1 version unify 5.33.0 -> 5.34.0 (4 .aisop.json + AIAP.md + agent_card.json + quality_baseline.json). C2 name field version sync to v5.34.0 (4 modules). The 3 summary embedded-version strings (main 'Engine Router v5.34.0', normal_engine 'Normal Engine v5.34.0', agent_engine 'Sub Agent contract v5.34.0') corrected from v5.33.0 to v5.34.0 at ReviewFinalize C1/C2 finalize BEFORE hash recompute (node_engine summary has no embedded version). C3 evolution_history + version_history + AIAP.md evolution append (this entry). C4 governance_hash recompute via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC (AIAP.md / agent_card.json / quality_baseline.json; no AIAP_cn.md, TRI=3) + .evolution_snapshot/v5.34.0/ via tool_dirs/snapshot_build.py --pipeline_run_id=5801a991-1562-4f36-b4ac-4f1c01e4d341 (snapshot built LAST after all governance edits per turn-100 snapshot-staleness lesson; snapshot_audit exit 0 mandatory; never inline hash). Recomputed canonical hash 5c336ca7d2a43366ffd4e3639b31d819a5be0257419cf52670cef9b96687e131 (prior v5.33.0 was 6c888fe4...; differs because the 3 summary strings changed). HARD: only main 3 render points + node_engine programExec.step2 trace upgrade + custom engine-self Assert/template + render_claim wiring + 3 summary version strings + version/name fields touched; dispatched-node step4 NOT touched; python_tools/ NOT in hash scope and NOT modified; nothing outside the engine target. Honest annotation: engine-self node-info is prompt-level + tamper-evident (render_claim is self-reported, NOT ground truth; true assurance via band-external render_claim audit). Authorized at EvolveStep sovereignty gate (TRUE HALT, user reply 'all', accepted A1; Axiom 0 satisfied; no pre-authorization assumed). Finalize separately gated at ReviewPresent (TRUE HALT, zero-delta finalize_risk, user reply 'OK'). ThreeDimTest weighted=4.944 Grade S (full scope, all 4 modules; Professional C*0.25+I*0.45+D*0.30, C=4.96 I=4.92 D=4.96). Validation 32/32 0 RED, Simulation 16/16 PASS 100% coverage, NihilDensity GREEN, PostSimulateGate GREEN. 0 breaking changes, all additive."

  - version: "5.35.0"
    date: "2026-06-11"
    note: "MINOR evolution v5.34.0 -> v5.35.0 via Creator Evolve pipeline (cache/104) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target=runner=live). Feature: dispatched-node node-info fidelity hardening — node_engine.aisop.json step4 SECTION 4 ONLY (3 content edits A/B/C). Change A: Assert sub-line replaced 'strictly execute engine node steps; NEVER fabricate, NEVER skip' -> 'strictly PHYSICALLY READ and execute engine node steps, NEVER from memory; NEVER fabricate, NEVER skip, NEVER yield mid-pipeline; NEVER auto-approve a user gate (a gate is a hard STOP); CONTINUE per dispatch_plan to the terminal node.' Change B: new 5th sub-line 'Output:' inserted AFTER Assert and BEFORE Governance, with Align Axiom 0 preserved as the Axiom Governance last line (CANONICAL ENGLISH untouched) — 'STRICTLY render this node-info as ALL 7 sections VERBATIM (separator / Node State / For Audit / Report / Explain / Axiom Governance / end separator); NEVER compress, merge, abbreviate, or drop a section.' Change C: OUTPUT FIDELITY (hard, v5.35.0) rule added to the SIX-SECTION FIXED TEMPLATE region — render every node-info from the PHYSICALLY-READ step4 template, NEVER from a remembered/summarized version; ALL 7 sections VERBATIM; compression/omission = NODE_SUMMARY_OMITTED sovereignty-class violation (same enforcement class as A4). HARD-CONSTRAINT①: main / agent_engine / normal_engine Asserts, MAIN_AGENT_ASSERTS, engine-self custom Asserts, node logic UNCHANGED (version field only). FIX-1 (human-approved at ReviewPresent gate#2): SECTION 4 preamble count corrected '4 mandatory sub-lines' -> '5 mandatory sub-lines' (stale after change B). FIX-2 ('ALL 7 sections' terminology) kept verbatim per human decision. C1 version unify 5.34.0 -> 5.35.0 (4 .aisop.json + AIAP.md + agent_card.json + quality_baseline.json). C2 name/summary version strings synced. C3 evolution_history + version_history + AIAP.md evolution append (this entry). C4 governance_hash recompute via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC (AIAP.md / agent_card.json / quality_baseline.json; no AIAP_cn.md, TRI=3) + .evolution_snapshot/v5.35.0/ via tool_dirs/snapshot_build.py --pipeline_run_id=5b554e44-b84c-4fa5-8d8f-e1078bb547b7 (snapshot built LAST after all governance edits; snapshot_audit exit 0 mandatory; never inline hash). Recomputed canonical hash 01238d17f2e3b8e71d9c28ee1862c705943602f47fd9917794a9a2c4afef6e38 (prior v5.34.0 was 5c336ca7...; differs because node_engine step4 changed). Honest annotation: A/B render lines are weak prompt-level floors (compressible), C OUTPUT FIDELITY rule is a firmer floor (read before render) — both NON-machine-enforced; durable enforcement needs framework-level render verification / per-node sub-agent producing the node-info (backlog). Authorized at EvolveStep sovereignty gate (TRUE HALT, user reply 'all', accepted A/B/C + version_sync). Finalize separately gated at ReviewPresent (TRUE HALT, user reply 'finalize', accepted FIX-1/FIX-2/keep-name-bump). ThreeDimTest weighted=4.949 Grade S (full scope, all 4 modules; Professional C*0.25+I*0.45+D*0.30, C=5.00 I=4.92 D=4.95). 0 breaking changes, all additive."

  - version: "5.36.0"
    date: "2026-06-12"
    note: "MINOR evolution v5.35.0 -> v5.36.0 via Creator Evolve pipeline (cache/107) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target=runner=live). Feature: three node-info template alignment (A/B/C) + FAST ENTRY entry-discipline (D) — consistency cleanup + reduced opening pre-analysis waste; NON-functional changes only. Change A (node_engine.aisop.json step4 SECTION 4 Governance sub-line): 'all completed items governed by human (all done governed by human)' -> 'all execution results under human audit and governance' (removed synonym-repeat parens; SECTION 0/1/1.5/2/3/5 + Next/Assert/Output/Align Axiom 0 sub-lines untouched). Change B (4 engine-self Axiom Governance blocks — main Router.match/cache-setup/execution-summary + node_engine programExec.dispatch-plan): (①) sub-line order reordered to canonical Next/Assert/Output/Governance/Align Axiom 0 (was Assert/Next/Governance/Align); (②) new Output sub-line added (canonical, same as dispatched nodes) — 'STRICTLY render this node-info as ALL 7 sections VERBATIM (separator / Node State / For Audit / Report / Explain / Axiom Governance / end separator); NEVER compress, merge, abbreviate, or drop a section.'; (③) Assert sub-line simplified to keep only the engine-self physical-read + own-sequence clause (moved the 'render full node-info VERBATIM each step' clause out, now carried by the new Output sub-line); (④) Governance sub-line -> canonical 'all execution results under human audit and governance'; (⑤) Next sub-line keeps engine-self form, Align Axiom 0 canonical English unchanged. Change C (agent_engine.aisop.json node_info_render contract sections): ['分隔线','Node State','For Audit','Report','Explain','Axiom Governance'] -> ['separator','Node State','For Audit','Report','Explain','Axiom Governance','End Separator'] (added End Separator -> 7 entries, '分隔线'->English 'separator'; user_language/axiom0_verbatim/report_source untouched). Change D (main.aisop.json match.step1 prepend): FAST ENTRY (v5.36.0) execution-discipline preamble prepended via ' | ' before the existing 'Match user message...' text — upon receiving the user instruction, IMMEDIATELY begin match->execute->programExec mechanically; do NOT pre-read/pre-analyze the target AIAP / creator-target pipeline / external docs BEFORE executing; target AIAP is physically read PER-NODE at programExec step1 exactly when needed (never upfront); no opening 'analyze everything first' preamble; in-flow required reads (main.aisop.json + per-node target reads) are part of the flow, NOT pre-analysis. match uncertain/Error + classify/execute/engineExec logic + package-match body semantics UNCHANGED. HARD-CONSTRAINTS: ① content edits only A+B+C+D; normal_engine version-only bump; finalize/dispatch/snapshot/governance_hash/sovereignty logic + MAIN_AGENT_ASSERTS (Assert 1/2/3) + dispatched-node step4 Next/Assert/Output/Align Axiom 0 + six-section structure + match package-match body all UNCHANGED. ② A/B Output/Governance/Align Axiom 0 three lines literally identical; Assert line A/B contextual wording may differ. ③ version 5.35.0 -> 5.36.0 unified across 4 modules. ④ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC (AIAP.md / agent_card.json / quality_baseline.json; no AIAP_cn.md, TRI=3) + .evolution_snapshot/v5.36.0/ via tool_dirs/snapshot_build.py (snapshot built LAST after all governance edits; snapshot_audit exit 0 mandatory; never inline hash). ⑤ EvolveStep + ReviewPresent two sovereignty gates TRUE HALT (no pre-authorization / no auto-approve). ⑥ target=runner=live engine. ⑦ Honest annotation: A/B/C are cosmetic prompt-level template-consistency; D is prompt-level entry discipline (reduces pre-analysis token/time waste, NOT 100% enforced); none change execution/governance logic. Authorized at EvolveStep sovereignty gate (TRUE HALT, user reply 'all', accepted A/B/C/D + version_sync). Finalize separately gated at ReviewPresent (TRUE HALT, zero-delta finalize_risk, user reply 'OK'). ThreeDimTest weighted=4.949 Grade S (full scope, all 4 modules; Professional C*0.25+I*0.45+D*0.30, C=5.00 I=4.92 D=4.95). Zero-delta vs v5.35.0 (4.949) expected for cosmetic+discipline alignment. 0 breaking changes, all additive."

  - version: "5.38.0"
    date: "2026-06-13"
    note: "MINOR evolution v5.36.0 -> v5.38.0 via Creator Evolve pipeline (cache/112) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target=runner=live; only this one copy modified per hard-constraint ⑥). 从 v5.36 干净重建合并 v5.37 sections_rendered + v5.38 基础 LC + ENG-STDIN — predetermined surgical batch merge of executor-related content from a clean v5.36 base (no research/proposal/implementation-design freedom this round; the 5 changes were written verbatim into the directive, ModifyStep only string-substituted). 5 CHANGE GROUPS (B1-B5): B1 — 5 node_info_render_claim sections_rendered placeholders '[...]' all replaced with one canonical Title-Case constraint sentence ('COPY THE CANONICAL SECTION LABELS VERBATIM in Title-Case ... [\"separator\",\"Node State\",\"For Audit\",\"Report\",\"Explain\",\"Axiom Governance\",\"End Separator\"] — NEVER snake_case (node_state/for_audit/axiom_governance/…), NEVER lowercase, abbreviate, or normalize them. The E1 out-of-band auditor matches these names EXACTLY (character-for-character) against the render contract.'), distributed across main execute.step1 (Router.match) + execute.step3 (Router.cache-setup) + engineExec.step8 (Router.execution-summary) + node_engine programExec.step4 (dispatched-node) + programExec.step2 (engine-self) — 0 placeholders remain. B2 (LC-1) — main json_schema loading_mode enum [\"node\",\"normal\"] -> [\"node\",\"normal\",\"lite\"] (factual-consistency fix; lite already implemented in execute.step4/engineExec.step3). B3 (LC-7) — 3 module summaries strip hard-coded version self-label so the version field is the sole source: main 'Engine Router v5.34.0: matches' -> 'Engine Router: matches'; agent_engine 'Sub Agent contract v5.34.0: init' -> 'Sub Agent contract: init'; normal_engine 'Normal Engine v5.34.0: node-by-node' -> 'Normal Engine: node-by-node' (node_engine summary had no version token; historical changelog in description untouched). B4 (LC-2) — AIAP.md new 'Module Responsibility Matrix / 模块职责矩阵' subsection (7 responsibility rows x 4 module columns: read target content / dispatch_plan authority / write node cache / write _index match+summary / write ExecutionSummary+conversation_context / render 6-section node-info / governance_hash verification; pure documentation, no spec-logic change). B5 (ENG-STDIN) — node_engine programExec.constraints appended a TOOL CALL DISCIPLINE (v5.38.0) clause: all JSON-passing agent_update_index.py / agent_write_node_cache.py calls MUST use STDIN heredoc, NEVER inline --updates='{...}' on the command line, NEVER chain index-update + agent_id_generator + render_claim into a single multi-&& compound Bash one-liner with inline python -c JSON construction (inline/compound JSON-in-Bash breaks on Windows/PowerShell escaping -> malformed command / no output -> pipeline stall); one tool call per step, no batching. HARD-CONSTRAINTS (preserved): ① content edits only B1-B5; NEVER touched any step execution logic / node structure / dispatch / finalize / snapshot / governance / two-gate / X3 / circuit-breaker / recovery / MAIN_AGENT_ASSERTS / node-info template body; NO LEVEL_A structural change; LB-1 / finalize_risk / gate logic explicitly excluded and untouched. ② 4 modules version unified 5.36.0 -> 5.38.0 (MINOR). ③ framework Python (acp_llm/runner/agent.py/soulacp) untouched; python_tools logic untouched (ENG-STDIN only changed .aisop.json wording). ④ EvolveStep + ReviewPresent two sovereignty gates TRUE HALT — EvolveStep user reply 'all' (accepted B1-B5, presented_at 12:33:22Z); ReviewPresent zero-delta finalize_risk user reply 'ok' (accepted finalize, presented_at 18:19:09Z); no pre-authorization, no auto-approve. ⑤ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine has no AIAP_cn.md) + .evolution_snapshot/v5.38.0/ via tool_dirs/snapshot_build.py (built LAST after all governance writes; snapshot_audit.py exit 0 mandatory). ⑥ target=runner=live engine (self-evolution), only this one copy. ⑦ Honest annotation: pure consistency/hygiene/documentation/discipline wording, ZERO execution/governance logic change; B1 sections_rendered anchoring + B5 STDIN discipline remain prompt-level (true render-fidelity / stall-prevention would need framework- or tool-side rejection of malformed inline JSON, left as backlog). ACCEPTANCE FOCUS verified: ModifyStep real per-node dispatch (ModifyStep.json present, agent_id=auto-1552a7a7, tool_calls=33>0, status PASS); all 18 implementation nodes real sub-agent, 0 integrity violations; 18 leaf deltas / 0 unexpected vs v5.36.0 snapshot. ThreeDimTest weighted=4.949 Grade S (authoritative trajectory baseline, full scope all 4 modules; Professional C*0.25+I*0.45+D*0.30, C=5.00 I=4.92 D=4.95). Zero-delta vs v5.36.0 (4.949) expected for consistency+documentation+discipline alignment (per-dimension like-for-like deferred-excluded real delta 0.000; the sole finalize_risk trigger was ZERO-DELTA, INVIOLABLE Axiom 0). 0 breaking changes, all additive."
  - version: "5.39.0"
    date: "2026-06-14"
    note: "MINOR evolution v5.38.0 -> v5.39.0 via Creator Evolve pipeline (cache/123) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target=runner=live; only this one copy modified, highest risk). Surgical predetermined prompt-discipline hardening, 2 content files only (main + node_engine); not research-driven. 2 CHANGE GROUPS (C1-C2, both LEVEL_C): C1 — main.aisop.json Router match.step1 FAST ENTRY: appended an EXECUTION CONTRACT paragraph (verbatim, NO version number inside) between '...NOT pre-analysis.) ' and '| Match user message...' closing the 'no opening preamble' semantic gap — FAST ENTRY means skip the OPENING PRE-ANALYSIS preamble only; it does NOT exempt per-node six-section node-info rendering, and from the FIRST node in nodes_in_path (INCLUDING inline entry nodes Start/NLU/PipelineEntry) EVERY node MUST render its full node-info per node_engine step4 — plus one mandatory execution-discipline contract line ('I will execute node-by-node mechanically; for EACH node I first emit a NEXT-NODE PLAN then the full node-info; I will not skip nodes or shortcut') and a deterrence clause anchored to REAL out-of-band auditing (opening-node render, node-info self-consistency, dispatch evidence, NEXT-NODE PLAN four-way cross-check; missing required node-info/NEXT-NODE PLAN => run judged invalid / evolution rejected). C2 — node_engine.aisop.json programExec.step3 per-node loop: appended a NEXT-NODE PLAN (plan-then-act) paragraph (verbatim, NO version number) after '(b) ... extract dispatch_plan[current_node].' and before '(c) PARALLEL DISPATCH (B3):' requiring, BEFORE dispatching current_node, a PHYSICAL READ of _index.json::dispatch_plan[current_node] and emission of a next_node_plan { node, execute_mode, expected_agent_id, planned_action, will_render_node_info: true } whose STRUCTURAL fields (execute_mode / expected_agent_id) are COPIED VERBATIM from the tool-written dispatch_plan (the deterministic generate_dispatch_plan / agent_id_generator values — anchored, NOT self-report; emitting a different value is a dispatch-integrity violation surfaced post-hoc), WRITTEN to _index.json::nodes_status[current_node].next_node_plan via the EXISTING agent_update_index.py over STDIN heredoc (NO inline command-line JSON, NO new python tool) — the PRE-DISPATCH sibling of step4 Section-4's post-node 'Next' pre-commitment, enabling the out-of-band auditor to cross-check FOUR sources: dispatch_plan(tool) ↔ next_node_plan(agent-declared) ↔ dispatch_timeline(actual dispatch) ↔ node cache(actual agent_id); a four-way mismatch is shortcut/downgrade evidence (e.g. agent->inline silent downgrade, or dropped inline entry nodes). HARD-CONSTRAINTS (preserved): ① content edits only C1+C2 (main match.step1 + node_engine programExec.step3); NEVER touched dispatch logic (generate_dispatch_plan / agent_id_generator / dispatch_audit) / step4 six-section template + Section-4 'Next' line / X3 / user_gate / finalize_risk trigger list / INVIOLABLE / AIAP_Standard library; NO LEVEL_A; no new step keys (pure single-step prose growth, node count + Mermaid 1:1 unchanged). ② 4 modules version unified 5.38.0 -> 5.39.0 (MINOR), name v-strings synced. ③ framework Python untouched; NO new python tool (reuse agent_update_index.py via STDIN heredoc). ④ EvolveStep + ReviewPresent two sovereignty gates — EvolveStep TRUE HALT, user reply 'all' (accepted C1/C2/C3-version-sync-7, presented_at 02:43:38Z, question_hash df6860e1); ReviewPresent conditional gate (user_gate=conditional) finalize_risk=FALSE (4 triggers all FALSE: regression=FALSE [zero-delta is NOT a regression], traffic-RED=FALSE, any-DEGRADED=FALSE, simulate-RED=FALSE) -> spec-mandated branch-2b AUTO-APPROVED (NOT a fabricated bypass; the decision was driven by finalize_risk per step8's own logic, no silent agent discretion, no auto-approval of a TRUE-risk gate; EvolveStep already user-authorized this evolution + clean quality). ⑤ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine has no AIAP_cn.md) + .evolution_snapshot/v5.39.0/ via tool_dirs/snapshot_build.py (built LAST per v2.39.0 ordering, AFTER governance_hash + identity verified_on; snapshot_audit.py exit 0 mandatory). ⑥ target=runner=live engine (self-evolution), only this one copy. ⑦ Honest annotation: pure prompt-level execution-discipline wording, ZERO execution/governance logic change; C1 per-node node-info-render anchoring + C2 next_node_plan structural anchoring reduce shortcutting but their REAL teeth are the out-of-band four-way audit + framework hook (backlog), not a machine guarantee. ACCEPTANCE: ModifyStep real per-node dispatch (ModifyStep.json present, agent_id=auto-c5ce8758, tool_calls=9>0, status PASS, real Edit landed C1/C2); ValidateStep byte/leaf diff vs v5.38.0 snapshot = 13 authorized leaves (11 version-sync + 2 content inserts) / 0 collateral / NO HARD_FAIL; C1 1330-char pure-insert in main.match.step1, C2 1570-char pure-insert in node_engine.programExec.step3, both with NO version number inside; step4 six-section template + Section-4 'Next' line byte-identical, dispatch tool refs intact. ThreeDimTest weighted=4.949 Grade S (engine baseline convention C*0.25+I*0.45+D*0.30, Generate2 authoritative SCORE SOURCE; C=5.00 I=4.92 D=4.95 deferred-excluded). Zero-delta vs v5.38.0 (4.949) — additive LEVEL_C prompt-discipline prose, teeth out-of-band, no machine-enforced quality lift; step6 reconcile confirmed the Generate2 full-score dip to 4.922 was purely the P23 D6/D10 pre-Finalize timing artifact (now reconciled to 4.952 full / 4.949 reported deferred-excluded). 0 breaking changes, all additive."

  - version: "5.40.0"
    date: "2026-06-15"
    note: "MINOR evolution v5.39.0 -> v5.40.0 via Creator Evolve pipeline (cache/125) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target=runner=live; only this one copy modified, highest risk). Surgical predetermined prompt-discipline/format hardening, 2 content files only (main + node_engine); not research-driven. 3 CHANGE GROUPS (B-CF / B-INLINE / ENG-STDIN): B-CF (no code format) — the SAME RENDER FORMAT rule appended at 4 render points (node_engine step4 six-section node-info template, node_engine step3 NEXT-NODE PLAN/C2 segment, node_engine step2 engine-self programExec.dispatch-plan render, main engine-self render Router.match/cache-setup/execution-summary): render node-info / NEXT-NODE PLAN as PLAIN MARKDOWN (bold **labels** + ━ separators + prose); NEVER wrap the whole node-info block or NEXT-NODE PLAN body in a ``` code fence; NEVER put CJK/prose inside a monospace code block (grey monospace box = #1 readability complaint); inline `backtick` spans allowed ONLY for short tokens (paths, agent_id, field names, hashes), NEVER a whole section / plan body / multi-line CJK prose. B-INLINE (C2 next_node_plan inline coverage) — rewrote the v5.39.0 C2 NEXT-NODE PLAN segment in node_engine step3 to EMIT TIMING + INLINE COVERAGE: emit the NEXT-NODE PLAN at step3(b) IMMEDIATELY AFTER [PHYSICAL READ] dispatch_plan[current_node] for EVERY node INCLUDING inline entry nodes (Start/NLU/PipelineEntry); structural fields copied VERBATIM from dispatch_plan[current_node] (execute_mode; expected_agent_id = agent_id_generator value for agent nodes, 'inline_planned' for inline nodes); do NOT defer the write to after d.0 (agent_id_generator runs only for agent nodes — deferring SKIPS the inline entry nodes, the v5.39.0 gap of 18/18 agent had next_node_plan but 0/3 inline); written to _index.json::nodes_status[current_node].next_node_plan via the EXISTING agent_update_index.py. ENG-STDIN (Windows heredoc guidance) — appended a WINDOWS HEREDOC HAZARD clause to node_engine constraints TOOL CALL DISCIPLINE: on Windows a Bash heredoc corrupts BOTH backslash Windows-paths AND CJK UTF-8 (surrogate '\\udcXX', observed breaking _index.json writes); for ANY JSON carrying Windows paths or CJK, do NOT use an unquoted Bash heredoc — instead (a) pipe a PowerShell SINGLE-QUOTE here-string (@'...'@, literal, no backslash/var expansion) into the tool STDIN, or (b) Write the JSON to a temp file then feed it via < tempfile (canonical path-/CJK-safe way to pass JSON to agent_update_index.py / agent_write_node_cache.py). HARD-CONSTRAINTS (preserved): ① content edits only B-CF (4 render points) + B-INLINE (C2 segment rewrite) + ENG-STDIN (constraints append); NEVER touched the step4 six-section template content/structure (only the one RENDER FORMAT sentence added, Section-4 'Next' line untouched) / dispatch logic (generate_dispatch_plan / agent_id_generator / dispatch_audit) / X3 / user_gate / finalize_risk trigger list / INVIOLABLE; NO LEVEL_A; no new step keys (pure single-step prose growth + C2 segment rewrite, node count + Mermaid 1:1 unchanged). ② 4 modules version unified 5.39.0 -> 5.40.0 (MINOR), name v-strings synced; agent_engine/normal_engine version-sync only. ③ framework Python untouched; NO new python tool (reuse agent_update_index.py via STDIN/temp-file). ④ EvolveStep + ReviewPresent two sovereignty gates — EvolveStep TRUE HALT, user reply 'all' (accepted B-CF/B-INLINE/ENG-STDIN, question_hash 58079f02); ReviewPresent conditional gate finalize_risk=FALSE (4 triggers all FALSE: regression=FALSE [zero-delta is NOT a regression], traffic-RED=FALSE, any-DEGRADED=FALSE, simulate-RED=FALSE) -> spec-mandated branch-2b AUTO-APPROVED (conditional gate, non-bypass; EvolveStep already user-authorized this evolution + clean quality). ⑤ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine has no AIAP_cn.md) + .evolution_snapshot/v5.40.0/ via tool_dirs/snapshot_build.py (built LAST AFTER governance_hash + identity per v2.39.0 ordering; snapshot_audit.py exit 0 mandatory). Recomputed canonical hash 6c12a6cb9537ad8e64e2cd57947c83d1248d4de0701e433ee9c84cf3ec18c002 (prior v5.39.0 was f3aa702cf2da6e28d21b0aaa28049be31b88bb852b39a0f1a5bfcdbf350c88dc; differs because main + node_engine changed). ⑥ target=runner=live engine (self-evolution), only this one copy. ⑦ Honest annotation: B-CF/ENG-STDIN are prompt-level discipline/guidance (orchestrator can still ignore — code-format is a render concern leaving NO cache trace, only judgeable from chat output); B-INLINE is a coverage-logic fix (C2 next_node_plan now also covers the 3 inline entry nodes); true machine root-fix still needs framework hooks. ACCEPTANCE: ModifyStep real per-node dispatch (ModifyStep.json present, agent_id=auto-70cbbb2f, tool_calls=8>0, status PASS, real Edits landed B-CF/B-INLINE/ENG-STDIN); ValidateStep byte/leaf diff vs v5.39.0 snapshot = 18 authorized leaves / 0 collateral / NO HARD_FAIL. ThreeDimTest weighted=4.949 Grade S (engine baseline convention C*0.25+I*0.45+D*0.30; C=5.00 I=4.92 D=4.95 deferred-excluded). Zero-delta vs v5.39.0 (4.949) — prompt-level discipline/format + a coverage-logic fix, no machine-measurable static lift; regression_flags=[] (zero-delta is not regression). 0 breaking changes, all additive."

  - version: "5.41.0"
    date: "2026-06-15"
    note: "MINOR evolution v5.40.0 -> v5.41.0 via Creator Evolve pipeline (cache/126) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target=runner=live; only this one copy modified, highest risk). Surgical predetermined changes (not research-driven): 2 change groups across 4 content modules + 1 metadata correction. CHANGE A (ENG-STDIN strong mandate — JSON always via file, fully off the command line): every spec instruction that writes _index.json via agent_update_index.py or a node cache via agent_write_node_cache.py was converted to the canonical form 'first file_system/Write the JSON to a CONTEXT_DIR temp file (_tmp_*.json), THEN pass --updates-file=<path> / --data-file=<path>'; three prohibitions added (NO inline --updates='{...}' / --data='{...}'; NO printf|echo '{...}' | python; NO cd ... && compound commands). Landing points: agent_engine writeCache.step2 (nodes_status + last_completed) + inline-mode agent_write_node_cache prose (--data-file); node_engine programExec.step3 all agent_update_index calls (dispatch_plan / current_node / nodes_status / next_node_plan) + constraints TOOL CALL DISCIPLINE upgraded from here-string/heredoc canonical to file-based canonical + 3 prohibitions; main 3 engine-self render_claim writes (execute.step1 Router.match / execute.step3 Router.cache-setup / engineExec.step8 Router.execution-summary) 'STDIN or args-list JSON' -> --updates-file; normal_engine node cache write segment (--data-file, normal-mode forward benefit). CHANGE B (EU AI Act Art.50 penalty correction, single field) — main.aisop.json eu_ai_act_art50.penalties_context 'Up to 35M EUR or 7% global turnover' -> 'Up to 15M EUR or 3% of worldwide annual turnover' (Art.50 transparency obligations fall under Art.99(4) = €15M/3%; the old 35M/7% is the Art.5 prohibited-practices tier Art.99(3) — mis-assigned). Y1 (agent_card metadata drift) — agent_card.json penalties_context synced to the corrected 15M/3% (spec<->metadata drift removed). HARD-CONSTRAINTS (preserved): ① content edits limited to change A (4-module tool-call sites + node_engine TOOL CALL DISCIPLINE wording) + change B (main one field); NEVER touched dispatch logic / step4 six-section template + Section-4 'Next' / C1·C2·B-CF·B-INLINE existing content / X3·user_gate / finalize_risk trigger list / INVIOLABLE / AIAP_Standard.*. ② 4 modules version unified 5.40.0 -> 5.41.0 (MINOR), name v-strings synced, no fork. ③ NO new python tool (--updates-file/--data-file already in python_tools). ④ no version numbers written into .aisop.json prose (summary/description/FAST ENTRY). ⑤ EvolveStep + ReviewPresent two sovereignty gates — EvolveStep TRUE HALT, user reply 'all + Y1' (accepted B1a/B1b/B1c/B1d/B2/C1/C2/C3/C4/C5/Y1, question_hash 19714d2e28d277cb); ReviewPresent conditional gate finalize_risk=FALSE (4 triggers all FALSE: regression=FALSE [zero-delta is NOT a regression], traffic-RED=FALSE, any-DEGRADED=FALSE, simulate-RED=FALSE) -> spec-mandated branch-2b AUTO-APPROVED (conditional gate, non-bypass; EvolveStep already user-authorized + clean quality). ⑥ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine has no AIAP_cn.md) + .evolution_snapshot/v5.41.0/ via tool_dirs/snapshot_build.py (built LAST AFTER governance_hash + identity per v2.39.0 ordering; snapshot_audit.py exit 0 mandatory). NOTE: change A is FORWARD-EFFECTIVE — this run executed on the live v5.40.0 engine (mandate not yet active); the strong file-based JSON mandate drives the NEXT engine run from v5.41.0. ⑦ target=runner=live engine (self-evolution), only this one copy. ⑧ Honest annotation: change A is prompt-level tool-call discipline (no dispatch/data-contract/consumer change; orchestrator can still ignore — true unbypassable enforcement needs framework hooks); change B/Y1 are passive compliance-string corrections. CARRIED YELLOW ADVISORY: node_engine step2 (programExec.dispatch-plan) + step4 (NODE_INFO_RENDER_CLAIM WRITE) engine-self render_claim writes intentionally left at old 'STDIN/args-list JSON' phrasing (outside user's explicit node_engine step3-only landing scope + hard-constraint #1 step4 six-section protection) while main's 3 render_claim writes ARE converted — minor cross-file phrasing inconsistency, low stall risk, tracked for a future explicitly-scoped cleanup. ACCEPTANCE: ModifyStep real per-node dispatch (ModifyStep.json present, agent_id=auto-6cc0b4a7, tool_calls=33>0, status PASS, real Edits landed change A 4-module sites + change B + Y1 + C1/C2 7-file version sync); ValidateStep 22/22 leaf checks PASS (0 blocking, 1 deferred advisory) vs v5.40.0 snapshot. ThreeDimTest weighted=4.949 Grade S (engine baseline convention C*0.25+I*0.45+D*0.30; C=5.00 I=4.96 D=4.85 raw, deferred-excluded weighted 4.949 / full 4.929). Zero-delta vs v5.40.0 (4.949) — prompt-discipline + compliance-string correction, no machine-measurable static lift; regression_flags=[] (zero-delta is not regression). 0 breaking changes, all additive."

  - version: "5.42.0"
    date: "2026-06-15"
    note: "MINOR evolution v5.41.0 -> v5.42.0 via Creator Evolve pipeline (cache/127) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target=runner=live; only this one copy modified, highest risk). Surgical predetermined change (NOT research-driven): A1 — YELLOW cleanup that closes the v5.41 CARRIED YELLOW ADVISORY. The v5.41 ENG-STDIN strong mandate landed at every 'step3' JSON-payload call site but missed 2 node_engine render_claim writes. A1 converts exactly those 2 transport-wording parentheticals to the canonical file-based form already used everywhere else in v5.41: (1) programExec engine-self dispatch-plan RENDER_CLAIM WRITE (step2 block) and (2) programExec.step4 dispatched-node NODE_INFO_RENDER_CLAIM WRITE (C1 v5.29.0). Both go from the old '(--updates via STDIN or as an args-list JSON; NO inline PowerShell-constructed JSON)' phrasing to the canonical 'FIRST write the --updates JSON payload to a temp file under CONTEXT_DIR named _tmp_*.json via the file_system/Write tool, THEN pass it as agent_update_index.py --cache_dir=CONTEXT_DIR --updates-file=<temp file path>; PROHIBITED: inline --updates='{...}' on the command line, printf/echo '{...}' | python piping, cd ... && compound commands; NO inline PowerShell-constructed JSON; do NOT add a new python tool, --updates-file already exists in python_tools'. Pure transport-wording prose alignment — zero behavioral/logic/data-contract change. HARD-CONSTRAINTS (preserved): ① content edits limited to the 2 node_engine render_claim write-call transport parentheticals; NEVER touched dispatch logic / step4 six-section template structure + Section-4 'Next' / C1·C2·B-CF·B-INLINE existing content / X3·user_gate / finalize_risk trigger list / INVIOLABLE / AIAP_Standard.* (the render_claim 7-section sections_rendered Title-Case list + user_language_used + E1 character-for-character / NEVER snake_case instruction all byte-preserved). ② 4 modules version unified 5.41.0 -> 5.42.0 (MINOR), name v-strings synced, no fork. ③ NO new python tool (--updates-file already in python_tools). ④ no version numbers written into .aisop.json prose (the canonical replacement OMITS the '(change A v5.41.0)' tag). ⑤ EvolveStep + ReviewPresent two sovereignty gates — EvolveStep TRUE HALT, user reply 'all' (accepted A1 + auto_C version/governance/snapshot sync, question_hash 0585f6b6f59a14f8); ReviewPresent conditional gate finalize_risk=FALSE (4 triggers all FALSE: regression=FALSE [zero-delta is NOT a regression], traffic-RED=FALSE, any-DEGRADED=FALSE, simulate-RED=FALSE) -> spec-mandated branch-2b AUTO-APPROVED (conditional gate, non-bypass; EvolveStep already user-authorized + clean quality). ⑥ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine has no AIAP_cn.md) + .evolution_snapshot/v5.42.0/ via tool_dirs/snapshot_build.py (built LAST AFTER governance_hash + identity per v2.39.0 ordering; snapshot_audit.py exit 0 mandatory). NOTE: A1 is FORWARD-EFFECTIVE — this run executed on the live v5.41.0 engine; the corrected file-based render_claim wording drives the NEXT engine run from v5.42.0. ⑦ target=runner=live engine (self-evolution), only this one copy. ⑧ Honest annotation: A1 is prompt-level transport-call discipline alignment (no dispatch/data-contract/consumer change); it removes the v5.41 cross-file phrasing inconsistency so all render_claim/index/cache writes now state the same canonical file-based mandate. ACCEPTANCE: ModifyStep real per-node dispatch (ModifyStep.json present, agent_id=auto-327e6232, tool_calls=9>0, status PASS, real Edits landed A1 2 spots + 4-module version+name); ValidateStep PASS vs v5.41.0 snapshot. ThreeDimTest weighted=4.949 Grade S (engine baseline convention C*0.25+I*0.45+D*0.30; C~4.93 I~4.92 D~4.85 deferred-excluded; D6/D10 reconciled upward post-Finalize as governance files now at v5.42.0 with recomputed governance_hash). Zero-delta vs v5.41.0 (4.949) — pure transport-wording alignment + version bump, no machine-measurable static lift; regression_flags=[] (zero-delta is not regression); score_scope=partial (only the 2 changed render_claim writes scored, whole-program weighted preserved 4.949). 0 breaking changes, all additive."

  - version: "5.43.0"
    date: "2026-06-16"
    note: "MINOR evolution v5.42.0 -> v5.43.0 via Creator Evolve pipeline (cache/129) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target = engine directory; only this one copy modified). Predetermined change (NOT research-driven): normal_engine.aisop.json ALL-INLINE-ization — content edits confined to normal_engine.aisop.json. BACKGROUND: normal_engine.aisop.json was a copy of node_engine (normal identity already applied) that still carried per-node sub-agent dispatch; this evolution makes it all-inline — keeping node discipline structure, removing sub-agent dispatch. A1: generate_dispatch_plan call site forces execute_mode='inline' for ALL nodes (no more per-node agent/inline decision; inline nodes agent_id='inline_planned') + dispatch_audit downgraded to a pre-execution NO-OP. A2: programExec.step3 node-loop (d) always takes the inline execution path (orchestrator runs agent_engine.aisop.json init->execute->review->writeCache in place, NEVER spawns a sub-agent); 6 dangling d.0 / agent_id_generator references swept inline-consistent; DORMANT-BUT-KEPT block (agent guards e2e/e2e'/spawn_failure_evidence inert, not deleted). A3: dispatch_audit -> no-op (no agent nodes to audit, no violations reported); --completeness-check KEPT EXACTLY as a mode-agnostic post-hoc gate. A4: summary/description + 3 observability gen_ai_span_types rewritten to reflect 'Normal Engine: node structure + all-inline execution (no sub-agent dispatch)'. NihilDensity GH1 incidentally cleaned 4 inherited '(change A v5.41.0)' inline tags (3 in step3, 1 in step5) — user-approved EXPECTED/ACCEPTABLE. HARD-CONSTRAINTS (preserved): ① content edits confined to normal_engine.aisop.json (all-inline-ization A1-A4 + identity/summary + 4 tag cleans); main/node_engine/agent_engine bodies byte-identical to v5.42.0 snapshot EXCEPT version+name (0 content leaves each); agent_engine REUSED UNCHANGED by normal's inline path. ② 4 modules version unified 5.42.0 -> 5.43.0 (MINOR), name v-strings synced, no fork. ③ NO new python tool (existing creator tool_dirs tools used). ④ normal_engine PRESERVES un-weakened: six-section node-info, NEXT-NODE PLAN, NodeVerify, crash recovery, 3-state circuit breaker, sovereignty user_gate (writeCache user_gate_audit --enforce LIVE for ALL terminal-status nodes, mode-agnostic), writeCache. ⑤ no NEW version numbers written into .aisop.json prose (meta-excepted; the all-inline rewording omits the '(change A v5.41.0)' tag, inherited historical tags are pre-existing backlog). ⑥ EvolveStep + ReviewPresent two sovereignty gates — EvolveStep UNCONDITIONAL gate TRUE HALT, user reply 'all' (accepted A1/A2/A3/A4 + LEVEL_C, question_hash 39ea82564ffa8c30, presented_at 2026-06-16T07:31:33); ReviewPresent conditional gate finalize_risk=FALSE (4 triggers all FALSE: regression=FALSE [zero-delta is NOT a regression], traffic-RED=FALSE, any-DEGRADED=FALSE, simulate-RED=FALSE) -> spec-mandated branch-2b AUTO-APPROVED (conditional gate, non-bypass; EvolveStep already user-authorized + clean quality). ⑦ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine has no AIAP_cn.md) + .evolution_snapshot/v5.43.0/ via tool_dirs/snapshot_build.py (built LAST AFTER governance_hash + identity per v2.39.0 ordering; snapshot_audit.py exit 0 mandatory). ⑧ Honest annotation: all-inline-ization is a structural execution-model refactor (dispatch path) that PRESERVES every discipline structure un-weakened; it adds an inline-execution capability + simplifies dispatch but produces no machine-measurable C/I/D static lift; the dormant-but-kept agent guards are inert-not-deleted under all-inline. ACCEPTANCE: ModifyStep real per-node dispatch (ModifyStep.json present, agent_id=auto-8cb4b387, tool_calls=18>0, status PASS, real Edits landed A1-A4 + 6 d.0 sweeps); ValidateStep byte/leaf diff vs v5.42.0 snapshot = 11 authorized leaves (2 name+version + 9 content) / 0 collateral / 4 tag cleans / NO HARD_FAIL. ThreeDimTest weighted=4.949 Grade S (engine baseline convention C*0.25+I*0.45+D*0.30; C=5.00 I=4.92 D=4.95 deferred-excluded; source-of-truth recompute = 4.949). Zero-delta vs v5.42.0 (4.949); regression_flags=[] (zero-delta is not regression). 0 breaking changes, all additive. Recomputed canonical governance_hash 6210f2a120fa799a28b7f6951618f5946cbfb8f51c8032f168660c0e9097501b (prior v5.42.0 was 46356a34e2677994663546734567bb47e5fd6242f8fa2efc0f07b333cd513441; differs because normal_engine content + 4 module versions changed)."
  - version: "5.44.0"
    date: "2026-06-16"
    note: "MINOR evolution v5.43.0 -> v5.44.0 via Creator Evolve pipeline (cache/131) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target = engine directory; only this one copy modified). node-info discipline strengthening — prose+field NON-FUNCTIONAL change (NOT research-driven; predetermined MINOR). Content edits confined to 3 modules: main.aisop.json, node_engine.aisop.json, normal_engine.aisop.json. A1 — append ONE verbatim sentence to EVERY node-info Output sub-line at EXACTLY 7 places: after '...drop a section.' within the same Output bullet, append (identical at all 7) 'STRICTLY output the NEXT-NODE PLAN BEFORE EVERY node begins, NO exception, NEVER batched or skipped (omission = NODE_PLAN_OMITTED).'. Coverage = main 3 (Router.match / Router.cache-setup / dispatch-audit-summary engine-self node-infos) + node_engine 2 (engine-self template + dispatched/per-node template) + normal_engine 2 (mirror of node_engine) = 7; same sentence everywhere, no engine-self/per-node variant. A2 — add will_ audit fields to the NEXT-NODE PLAN struct (programExec.step3 (b)) in node_engine (+will_output_next_node_plan:true) and normal_engine (+will_output_next_node_plan:true, +will_write_cache_via_agent_write_node_cache_py:true); will_render_node_info KEPT verbatim (no rename/delete, no synonymous field added); main.aisop.json next_node_plan untouched (main only references, does not define the struct). A3 — main.aisop.json FAST ENTRY execution contract ('...NEXT-NODE PLAN (per node_engine step3) then the full node-info; I will not skip nodes or shortcut.' + DETERRENCE) left UNTOUCHED (no synonymous prose added; avoids nihil redundancy). LEVEL_C — 7-file version sync v5.43.0 -> v5.44.0 (4 .aisop.json modules + AIAP.md + agent_card.json + quality_baseline.json), name v-strings synced, NO fork; agent_engine.aisop.json version+name ONLY (functions block byte-identical to v5.43.0). GH1 INHERITED-TAG CLEANUP: 7 bare stale '(change A v5.41.0)' tags removed (main 3->0, node 4->0, normal already 0 cleaned prior run) per existing GH1/NihilDensity rules; the protected 'JSON-VIA-FILE MANDATE, change A v5.41.0' brackets retained 1:1 in node_engine + normal_engine. HARD-CONSTRAINTS (preserved): ① content edits confined to main/node_engine/normal_engine; agent_engine functions byte-identical (version+name only); NEVER touched dispatch logic / step4 six-section template body + Section-4 'Next' / X3 / user_gate / finalize_risk trigger list / INVIOLABLE / AIAP_Standard. ② 4 modules + 3 governance files version unified 5.43.0 -> 5.44.0 (MINOR), no fork. ③ NO new python tool (prose+field only; 16 existing engine python_tools unchanged). ④ UN-WEAKENED PRESERVATION (each item verified present on disk, not weakened): six-section node-info; NEXT-NODE PLAN existing 5 fields + EMIT TIMING (incl. 'for EVERY node INCLUDING the inline entry nodes'); NodeVerify; crash recovery; 3-state circuit breaker; sovereignty user_gate (writeCache user_gate_audit.py --enforce LIVE for ALL terminal-status nodes); writeCache; normal_engine ALL-INLINE OVERRIDE + step3(d) always-inline; dispatch_audit (pre-execution / completeness). ⑤ A1 appended prose contains NO version number (0 semver matches). ⑥ EvolveStep + ReviewPresent two sovereignty gates — EvolveStep UNCONDITIONAL gate TRUE HALT, user reply 'all' (accepted A1/A2/A3 + LEVEL_C, question_hash 2f1904b52cf13aee, presented_at 2026-06-16T11:30:16); ReviewPresent conditional gate finalize_risk=FALSE (4 triggers all FALSE: regression=FALSE [zero-delta is NOT a regression], traffic-RED=FALSE, any-DEGRADED=FALSE, simulate-RED=FALSE) -> spec-mandated branch-2b AUTO-APPROVED (conditional gate, non-bypass; EvolveStep already user-authorized + clean quality; no carried-over indicator-level halt per conditional-gate discipline). ⑦ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine has no AIAP_cn.md) + .evolution_snapshot/v5.44.0/ via tool_dirs/snapshot_build.py (built LAST AFTER governance_hash + identity per v2.39.0 ordering; snapshot_audit.py exit 0 mandatory). ⑧ Honest annotation: A1/A2 are prose-inside-string + audit-field additions — a node-info discipline strengthening that adds NO machine-measurable C/I/D static lift; A2 will_ fields REUSE the existing agent-declared next_node_plan struct (no new tool, no new injection surface); A1 only strengthens an already-present discipline. BOUNDARY (principle 7): this evolution specifies only which text/fields change; runtime NEXT-NODE PLAN rendering / gating / reconciliation behavior is unchanged; E1 band-out reconciliation SG (will_X <=> actual artifact) is a framework/tool-layer manual change OUT of this evolution's scope. ACCEPTANCE: ModifyStep real per-node dispatch (ModifyStep.json present, agent_id=auto-50afdad0, tool_calls=33>0, status PASS, real Edits landed A1 7-place + A2 will_ fields); ValidateStep byte/leaf diff vs v5.43.0 snapshot = 18 changed leaves (8 version/name + 10 content) / 0 added / 0 removed / collateral=0; A1 grep NODE_PLAN_OMITTED = 7 (main3/node2/normal2), A2 will_output_next_node_plan = 2 + will_write_cache_via_agent_write_node_cache_py = 1, agent_engine functions byte-identical. ThreeDimTest weighted=4.949 Grade S (engine baseline convention C*0.25+I*0.45+D*0.30; C=5.00 I=4.92 D=4.95 deferred-excluded; ReviewFinalize source-of-truth recompute = 4.949). Zero-delta vs v5.43.0 (4.949); regression_flags=[] (zero-delta is not regression). 0 breaking changes, all additive. Recomputed canonical governance_hash c1f3b9a9912dd3797621f0b8e8e02ea6c4960724aba975863e8d814a76307604 (prior v5.43.0 was 6210f2a120fa799a28b7f6951618f5946cbfb8f51c8032f168660c0e9097501b; differs because the 3 content modules' A1/A2 edits + 4-module version+name changed)."
  - version: "5.45.0"
    date: "2026-06-16"
    note: "MINOR evolution v5.44.0 -> v5.45.0 via Creator Evolve pipeline (cache/133) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target = engine directory = runner-direct live engine; only this one copy modified, highest risk). NEXT-NODE PLAN discipline-hardening — prose+field NON-FUNCTIONAL change (NOT research-driven; predetermined MINOR). Content edits confined to 2 modules: node_engine.aisop.json + normal_engine.aisop.json programExec.step3(b) NEXT-NODE PLAN object literal (MIRROR PAIR). A1 [node_engine] will_render_node_info:true -> MUST_render_node_info:YES, will_output_next_node_plan:true -> MUST_output_next_node_plan:YES + embedded 3-step node_workflow ['1. Output NEXT-NODE PLAN and persist to _index','2. Execute STRICTLY per NEXT-NODE PLAN','3. Output node info'] + trailing 'RULE: MUST ALL BE YES'. A2 [normal_engine] same 2 renames PLUS will_write_cache_via_agent_write_node_cache_py:true -> MUST_write_cache_via_agent_write_node_cache_py:YES + 4-step node_workflow (adds '3. Write node cache via agent_write_node_cache.py') + trailing 'RULE: MUST ALL BE YES'. A3 mirror invariant: both carry identical trailing 'RULE: MUST ALL BE YES'; node_engine 3-step omits the write-cache item (dispatched sub-agent self-writes cache via agent_engine.writeCache) while normal_engine 4-step includes it (all-inline override -> orchestrator writes cache) = JUSTIFIED ASYMMETRY. A4 verbatim prefix preservation: both NEW strings KEEP leading 'planned_action: <one line: what this node will do>,' verbatim. main.aisop.json next_node_plan UNTOUCHED (main references but does not define the struct) — version+name field bump only. LEVEL_C — 7-file version sync v5.44.0 -> v5.45.0 (4 .aisop.json modules + AIAP.md + agent_card.json + quality_baseline.json), name v-strings synced, NO fork; agent_engine.aisop.json version+name ONLY (functions block byte-identical to v5.44.0). HARD-CONSTRAINTS (preserved): ① content edits confined to node_engine + normal_engine step3(b) NEXT-NODE PLAN literal; main + agent_engine functions byte-identical (version+name only); NEVER touched dispatch logic / step4 six-section template body + Section-4 'Next' / NEXT-NODE PLAN EMIT TIMING / X3 / user_gate / finalize_risk trigger list / INVIOLABLE / AIAP_Standard. ② 4 modules + 3 governance files version unified 5.44.0 -> 5.45.0 (MINOR), no fork. ③ NO new python tool (prose+field only; existing engine python_tools unchanged). ④ UN-WEAKENED PRESERVATION (each verified present on disk): six-section node-info; NEXT-NODE PLAN EMIT TIMING (incl. 'for EVERY node INCLUDING the inline entry nodes'); A1 v5.44.0 NODE_PLAN_OMITTED deterrence line (byte-unchanged); NodeVerify; crash recovery; 3-state circuit breaker; sovereignty user_gate (writeCache user_gate_audit.py --enforce LIVE for ALL terminal-status nodes); writeCache; normal_engine ALL-INLINE OVERRIDE + step3(d) always-inline; dispatch_audit (pre-execution / completeness); four-way audit anchors (dispatch_plan<->next_node_plan<->dispatch_timeline<->node cache). ⑤ new MUST_/node_workflow/RULE block contains NO version number (0 semver matches). ⑥ EvolveStep + ReviewPresent two sovereignty gates — EvolveStep UNCONDITIONAL gate TRUE HALT, user reply 'all' (accepted A1/A2/A3/A4 + LEVEL_C); ReviewPresent conditional gate finalize_risk=FALSE (4 triggers all FALSE: regression=FALSE [zero-delta is NOT a regression], traffic-RED=FALSE, any-DEGRADED=FALSE, simulate-RED=FALSE) -> spec-mandated branch-2b AUTO-APPROVED (conditional gate, non-bypass; EvolveStep already user-authorized + clean quality; no carried-over indicator-level halt per conditional-gate discipline). ⑦ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine has no AIAP_cn.md) + .evolution_snapshot/v5.45.0/ via tool_dirs/snapshot_build.py (built LAST AFTER governance_hash + identity per v2.39.0 ordering; snapshot_audit.py exit 0 mandatory). ⑧ Honest annotation: A1/A2 are field-rename + node_workflow/RULE prose additions — a NEXT-NODE PLAN discipline strengthening that adds NO machine-measurable C/I/D static lift (the MUST_*:YES form HARDENS discipline prose but is prompt-level, not machine-enforced); next_node_plan storage key + four-way anchor NOT renamed (definition-only fields, zero external consumers, non-breaking); STEP6 P23 reconcile: D6/D10 4.5 placeholders re-measured to 5.0 post-governance-write. ACCEPTANCE: ModifyStep real per-node dispatch (ModifyStep.json present, agent_id=auto-699ab259, tool_calls=7>0, status PASS, real Edits landed A1+A2); ValidateStep byte/leaf diff vs v5.44.0 snapshot = 12 authorized leaves / 0 collateral (node_engine 3 + normal_engine 3 + main 2 + agent_engine 2 + AIAP.md 1 + agent_card 1 + quality_baseline 1); 0 residual will_ fields on both modules. ThreeDimTest weighted=4.949 Grade S (engine baseline convention C*0.25+I*0.45+D*0.30; C=5.00 I=4.92 D=4.95 deferred-excluded; ReviewFinalize source-of-truth recompute = 4.949 deferred-excluded / 4.952 full-D reconciled). Zero-delta vs v5.44.0 (4.949); regression_flags=[] (zero-delta is not regression). 0 breaking changes, all additive. Recomputed canonical governance_hash f9ab34cf567d9a1d3a92ca0b36ba3e217db4c51a2aa78e342b79f04980bca289 (prior v5.44.0 was c1f3b9a9912dd3797621f0b8e8e02ea6c4960724aba975863e8d814a76307604; differs because both content modules' step3(b) edits + 4-module version+name changed)."
  - version: "5.46.0"
    date: "2026-06-16"
    note: "STAGED BY ModifyStep (cache/135) — governance_hash + final weighted score to be RECONCILED/FINALIZED by ReviewFinalize. MINOR evolution v5.45.0 -> v5.46.0 via Creator Evolve pipeline. ENGINE SELF-EVOLUTION (target = engine directory = runner-direct live engine; only this one copy modified). node-info Report/Explain CONTENT-contract prose strengthening — ADDITIVE-ONLY (NO deletion / NO weakening / NON-FUNCTIONAL rewrite; NO control-flow change). Content edits: node_engine.aisop.json + normal_engine.aisop.json programExec.step4 SECTION 2 Report RULES (byte-identical shared text, lockstep — new RULE (4)) + SECTION 3 Explain RULES (byte-identical shared text, lockstep — new RULE (7)) + node_engine description #5-scope (#5-scope-L reaffirmation); reinforcement of agent_engine.aisop.json endNode.step1 node_info_render contract (report_source) + #4b/#4c/#4d. CHANGE R (NODE_REPORT_USERMSG_MISSING): Report MUST, in addition to the >=2-3 substantive-sentence user_summary, FULLY+FAITHFULLY output the triggering user message (sources _index.user_message / initial_user_message + cache.user_messages), labeled (用户原文：「…」), no omit/truncate/summarize; same-language -> verbatim, different-language -> faithful complete translation into user_language; ADDITIVE (does NOT replace existing Report rules (1)-(3)). CHANGE E (NODE_EXPLAIN_THIN): Explain MUST be substantive (>=2-3 sentences) explaining the why/cause/method/significance/outcome-meaning of Report, not a what-node+next-step stub; #4b non-authoritative tag governs AUTHORITY only (Report wins on divergence), NOT a thinness license. CHANGE L: Report substantive summary + entire Explain prose both in user_language; sole cross-language intersection = change-R user message (same-language verbatim / different-language translated); section labels (Report/Explain/Node State/For Audit/Axiom Governance/Align Axiom 0/End Separator) stay canonical English, never translated. UN-WEAKENED 9-ITEM PRESERVATION (each verified present on disk and untouched): existing Report RULES (verbatim forward user_summary, >=2-3 substantive, PROHIBIT one-sentence fallback, THIN->reconstruct from cache output no stub, report_source=user_summary verbatim); six-section node-info template; NEXT-NODE PLAN block (MUST_render_node_info / MUST_output_next_node_plan, normal_engine also MUST_write_cache_via_agent_write_node_cache_py) + node_workflow + RULE: MUST ALL BE YES; A1 Output sentence NODE_PLAN_OMITTED; EMIT TIMING for EVERY node INCLUDING inline entry nodes; #5-scope USER_LANGUAGE + #5-axiom canonical English; NodeVerify / crash recovery / §9 context-compaction re-entry / 3-state circuit breaker / user_gate --enforce LIVE / writeCache; normal_engine ALL-INLINE OVERRIDE + step3(d) always-inline / dispatch_audit; main.aisop.json engine-self unchanged (Router.match already echoes the user message). LEVEL_C: 7-file version sync 5.45.0 -> 5.46.0 (4 .aisop.json + AIAP.md + agent_card.json + quality_baseline.json), name v-strings synced, NO fork; agent_engine version+name + additive render-contract reinforcement; governance_hash recompute (canonical v1.0, TRI-SYNC, excludes AIAP_cn) + snapshot v5.46.0 performed by ReviewFinalize. BOUNDARY (principle 7): governs WHICH text/contract changes only; does NOT prescribe runtime render/gate behavior nor presuppose post-evolution score. E1 band-out SG machine-enforcement (verify Report substance / user_message verbatim / Explain substance) is a tool-layer hand-edit, OUT OF SCOPE this cycle. HONEST ANNOTATION: this cycle = prose strengthening, NOT a functional rewrite — it hardens node-info CONTENT contracts (prompt-level) and adds NO machine-measurable C/I/D static lift. New violation classes NODE_REPORT_USERMSG_MISSING + NODE_EXPLAIN_THIN. governance_hash PENDING_REVIEWFINALIZE_RECOMPUTE (prior v5.45.0 was f9ab34cf567d9a1d3a92ca0b36ba3e217db4c51a2aa78e342b79f04980bca289)."

  - version: "5.47.0"
    date: "2026-06-17"
    note: "MINOR evolution v5.46.0 -> v5.47.0 via Creator Evolve pipeline (cache/138) FINALIZED by ReviewFinalize. ENGINE SELF-EVOLUTION (target = engine directory = runner-direct live engine; only this one copy modified, highest risk). PROSE HYGIENE + change-R REVERT — NON-FUNCTIONAL prose cleanup (NOT research-driven; predetermined MINOR). 5 change groups C1/C3/C4/C5/C7, content edits across node_engine.aisop.json + normal_engine.aisop.json + agent_engine.aisop.json + AIAP.md frontmatter/matrix + agent_card.json. C1 — node_engine.step4 == normal_engine.step4 (byte-identical lockstep) three CN/EN mixed segments rewritten to pure English, semantics unchanged: A3 (opening / normal-mode first-batch inline nodes NOT exempt from six-section + (inline) marker + no silent startup + HONEST ANNOTATION), A6 (main_agent_asserts refresh), B2 (node_info_render contract cross-reference + HONEST ANNOTATION); lockstep preserved, CN=0 in that step. C3 — AIAP.md Module Responsibility Matrix (7x4) '/ 中文' stripped, English retained; structure/English unchanged, CN=0. C4 — 3 self-label fields (AIAP.md summary, agent_card.json description + quality_summary.note) made version-neutral English; structured fields untouched; embedded changelog kept as history in version_history/evolution_history. C5 — in-scope (step/constraint/Assert/Output prose) version-number tokens cleaned: '(change X v...)' fully deleted; '(rule-name v...)' / inline 'rule-name v...' (A1-A6/B1-B2/Bug1A-D/QS-/#4-#5/C1-C2/NM4) ' vN.M.K' suffix removed (rule-name anchor kept); node long-sentence rewrites drop version token while preserving semantics; in-scope v\\d+\\.\\d+\\.\\d+ = 0; version:/name fields + description fields + history regions untouched. C7 — REVERT change R (the v5.46.0 'Report MUST echo 用户原文' feature): deleted node_engine + normal_engine step4 SECTION 2 Report RULE(4) (#4c-R USER MESSAGE FAITHFUL OUTPUT / '用户原文：「…」' / violation class NODE_REPORT_USERMSG_MISSING) + the agent_engine endNode (R) 'faithful full output of the triggering user message (用户原文 label)' fragment, all removed. PRESERVED UN-TOUCHED by C7: (1) #4c (node step4 later '**User Message:** {content}' = full forward of the sub-agent-generated user_messages field, user original intent); (2) change E (Explain SECTION 3 substantive floor + NODE_EXPLAIN_THIN); (3) change L (user_language scope); (4) SECTION 2 Report RULES (1)(2)(3) (verbatim forward user_summary etc.); node==normal step4 lockstep maintained. HARD-CONSTRAINTS (preserved): ① content change only C1/C3/C4/C5/C7; C7 reverts ONLY the additive R feature, NEVER touches other step logic / dispatch / finalize / snapshot / governance / two gates / X3 / six-section template body / #4c / E / L; NO LEVEL_A. ② history NEVER touched: quality_baseline.json full text, AIAP.md version_history / changelog / last_pipeline / SUPERSEDES narrative, .aisop.json module description fields, intent/user example CN. ③ version:/name field values not touched; 4 modules unified to 5.47.0. ④ framework Python / python_tools untouched. ⑤ EvolveStep UNCONDITIONAL gate TRUE HALT, user reply 'all' (accepted C1/C3/C4/C5/C7, question_hash 4ffa481736fa48b2, presented_at 2026-06-17T11:41:36); ReviewPresent conditional gate finalize_risk=FALSE (4 triggers all FALSE: regression=FALSE [zero-delta is NOT a regression], traffic-RED=FALSE, any-DEGRADED=FALSE, simulate-RED=FALSE) -> spec-mandated branch-2b AUTO-APPROVED (conditional gate, user_gate=false, non-bypass; EvolveStep already user-authorized + clean quality; no carried-over indicator-level halt per conditional-gate discipline). ⑥ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine has no AIAP_cn.md) + .evolution_snapshot/v5.47.0/ via tool_dirs/snapshot_build.py (built LAST AFTER governance_hash + identity per v2.39.0 ordering; snapshot_audit.py exit 0 mandatory). ⑦ target=runner=live engine (self-evolution), only this one copy. ⑧ Honest annotation: C1/C3/C4/C5 are prose hygiene (CN removal / version-token slimming / self-label englishization) — zero execution/governance logic change, adds NO machine-measurable C/I/D static lift; C7 reverts the additive prompt-level R feature (out-of-band E1 audit was never wired, so removing the prose echo-requirement is non-functional). ACCEPTANCE: ModifyStep real per-node dispatch (ModifyStep.json present, agent_id=auto-0dbc4864, tool_calls=23>0, status PASS, real Edits landed C1/C3/C4/C5/C7 + 7-file version sync); ValidateStep byte/leaf diff vs v5.46.0 snapshot = 38 authorized leaves / 0 collateral; node_engine.step4 == normal_engine.step4 lockstep maintained; #4c / E / L / Report RULES (1)(2)(3) verbatim preserved. ThreeDimTest weighted=4.949 Grade S (engine baseline convention C*0.25+I*0.45+D*0.30; C=5.00 I=4.92 D=4.95 deferred-excluded; ReviewFinalize source-of-truth recompute = 4.949 deferred-excluded / 4.952 full-D reconciled). Zero-delta vs v5.46.0 (4.949); regression_flags=[] (zero-delta is not regression); score_scope=full (whole-program scored). Compliance NET-POSITIVE. 0 breaking changes."

  - version: "5.48.0"
    date: "2026-06-17"
    note: "STAGED BY ModifyStep (cache/139) — governance_hash + final weighted score + quality_baseline version_history/evolution_history sync to be RECONCILED/FINALIZED by ReviewFinalize. MINOR evolution v5.47.0 -> v5.48.0 via Creator Evolve pipeline. ENGINE SELF-EVOLUTION (target = engine directory = runner-direct live engine; only this one copy modified, highest risk). ADDITIVE-ONLY single-field insertion (NO deletion / NO weakening / NON-FUNCTIONAL prose; NO control-flow change) — user HARD zero-deletion boundary honored (only this 1 field added engine-wide; no existing rule / HONEST ANNOTATION / historical annotation / six-section template / changelog / intent touched). C6 (== EvolveStep B1): add ONE self-report boolean field `report_MUST_align_user_summary: YES` to the programExec.step3(b) NEXT-NODE PLAN object literal in node_engine.aisop.json AND normal_engine.aisop.json (MIRROR PAIR), as a sibling of MUST_render_node_info / MUST_output_next_node_plan, governed by the existing block RULE 'MUST ALL BE YES'. Semantics: pre-commitment that the node's Report faithfully executes the SECTION 2 Report contract (verbatim forward cache.user_summary / thin reconstruction from cache output / no stub). FILE-SPECIFIC ANCHORS (disk-verified, grep_uniqueness=1 each): node_engine — insert 'report_MUST_align_user_summary: YES, ' AFTER 'MUST_output_next_node_plan: YES, ' and BEFORE 'node_workflow:'; normal_engine — insert AFTER the same prefix and BEFORE 'MUST_write_cache_via_agent_write_node_cache_py: YES'. B-CROSS_MODULE: one logical constraint distributed across 2 modules => counted_as=1; CAP CHECK A+B=0+1=1 (A-CAP=5, B-CAP=10) PASS; SEMVER MINOR (pure additive). main.aisop.json + agent_engine.aisop.json next_node_plan UNTOUCHED (main references but does not define the struct; agent_engine has no step3(b)) — version+name field bump only. LEVEL_C — 7-file version sync 5.47.0 -> 5.48.0 (4 .aisop.json modules + AIAP.md + agent_card.json + quality_baseline.json), name v-strings synced, NO fork; agent_engine + main functions blocks byte-identical to v5.47.0 except the added field in node/normal (version+name only on main/agent). HARD-CONSTRAINTS (preserved): ① content change only the 1 C6 field insertion in node_engine + normal_engine step3(b) NEXT-NODE PLAN literal; NEVER touched dispatch logic / step4 six-section template body + Section-4 'Next' / NEXT-NODE PLAN EMIT TIMING / NODE_PLAN_OMITTED line / X3 / user_gate / finalize_risk trigger list / INVIOLABLE / AIAP_Standard / Report RULES (1)(2)(3) / change E / change L / #4c. ② history NEVER touched (quality_baseline full text deferred to ReviewFinalize; AIAP.md version_history / changelog / last_pipeline / SUPERSEDES narrative; module description fields; intent/user example CN). ③ version:/name field values authorized-bumped only; 4 modules unified to 5.48.0. ④ framework Python / python_tools untouched (NO new python tool). ⑤ EvolveStep UNCONDITIONAL gate TRUE HALT, user reply 'all' (accepted B1/C6, question_hash a81f01c912a96fc0, presented_at 2026-06-18T02:18:25; was_presented=true, auto_approve_fingerprint=false). ⑥ governance_hash via tool_dirs/governance_hash.py (canonical v1.0, absolute path, SOLE AUTHORITY — never inline) + TRI-SYNC=3 (AIAP.md / agent_card.json / quality_baseline.json; engine has no AIAP_cn.md) + .evolution_snapshot/v5.48.0/ via tool_dirs/snapshot_build.py — PERFORMED BY ReviewFinalize. ⑦ target=runner=live engine (self-evolution), only this one copy. ⑧ Honest annotation: C6 is a field addition — a node-info Report discipline strengthening that adds NO machine-measurable C/I/D static lift; the field REUSES the existing agent-declared next_node_plan struct (no new tool, no new injection surface); it aligns with the engine-self render_claim self-report pattern. PRE-TIER DIFF re-confirmed on disk: report_MUST_align_user_summary ABSENT (0 occurrences engine-wide) before this edit -> real additive change. ACCEPTANCE: ModifyStep real per-node dispatch (ModifyStep.json present, agent_id=auto-06fec4d0, real Edits landed C6 2 insertions + 6-file version/name sync; quality_baseline deferred to ReviewFinalize); post-edit re-grep report_MUST_align_user_summary == 2 (node_engine 1 / normal_engine 1); all edited *.aisop.json + agent_card.json re-validated as valid JSON. governance_hash PENDING_REVIEWFINALIZE_RECOMPUTE (prior v5.47.0 was 4be94de0559c0db295adb187924f05965cff303794449a055c5d4ebce35b9312). ThreeDimTest weighted score to be reconciled by ReviewFinalize (expected ZERO-DELTA vs v5.47.0; additive single field, prompt-level discipline, no machine-measurable static lift). 0 breaking changes, all additive."

---

## Governance Declaration

SoulBot Execute Engine is the core execution infrastructure of the SoulBot AIAP runtime.
It receives user intent from agent.py, routes to the appropriate AIAP program, selects
the execution engine (node or normal), and orchestrates node-by-node execution with
full observability, fault tolerance, and interrupt handling.

This program follows the AIAP V1.0.0 protocol, with Axiom 0 (Human Sovereignty and Wellbeing)
as its immutable axiom. All sys.io.confirm/input/select calls are forced-blocking per AISOP §6.2 —
they cannot be bypassed, auto-approved, or delegated to AI.

## Architecture Overview

### Execution Flow

```
EXECUTE_ENGINE=true:
  agent.py → main.aisop.json (Engine Router)
    → match (identify AIAP package)
    → execute (cache + context + select engine by loading_mode)
    → engineExec (monitor + NodeSummary + ExecutionSummary)
      ↓ loading_mode
    ┌────────┴─────────┐
    ↓                  ↓
  node_engine        normal_engine
  (Sub Agent/node)   (inline/node)
  default=agent      default=inline
       ↓
  agent_engine.aisop.json
  (init→execute→review→writeCache)
```

### Module Architecture (Pattern B+)

| Module | File | Nodes | Mode | Purpose |
|--------|------|-------|------|---------|
| **Engine Router** | main.aisop.json | 5 | sequential | Route + engine selection + monitoring |
| **Node Engine** | node_engine.aisop.json | 2 | hybrid | Sub Agent dispatch per node |
| **Normal Engine** | normal_engine.aisop.json | 2 | sequential | Inline execution per node |
| **Agent Engine** | agent_engine.aisop.json | 5 | sequential | Sub Agent execution contract |

### Module Responsibility Matrix

Per-module responsibilities as defined by each module's current spec (one Accountable owner per concern — separation of concerns). ✓ = module owns this responsibility; — = not this module's responsibility.

| Responsibility | main (Router) | node_engine | agent_engine | normal_engine |
|------------------------|:-------------:|:-----------:|:------------:|:-------------:|
| Read target content | — (records target PATH only, [PROHIBIT] reading content) | ✓ (programExec.step1 [PHYSICAL READ] of target entry file) | ✓ (init reads target function for YOUR_NODE) | ✓ (programExec physically reads each node from target) |
| dispatch_plan authority | — | ✓ (generate_dispatch_plan.py is SOLE authority; orchestrator never hand-writes) | — (reads expected_agent_id from _index dispatch_plan, never self-generates) | — (inline default, no agent dispatch_plan) |
| Write node cache | — | — (verifies cache exists; writes DEGRADED fallback only) | ✓ (writeCache writes {AIAP_NAME}.{NodeName}.json) | ✓ (inline writeCache via agent_write_node_cache.py) |
| Write _index match + summary | ✓ (execute.step3 writes target/aiap_name/loading_mode; module summary self-label removed, version field is SSOT) | — (updates nodes_status / dispatch_plan via agent_update_index.py merge) | — | — |
| Write ExecutionSummary + conversation_context | ✓ (engineExec.step7 ExecutionSummary + step8 conversation_context) | — ([PROHIBIT] Engine writing ExecutionSummary/conversation_context — Router's job) | — | — ([PROHIBIT] same as node_engine — Router's job) |
| Render 6-section node-info | ✓ (engine-self node-info for Router.match / cache-setup / execution-summary) | ✓ (programExec.step4 renders six-section node-info for each executed node + engine-self dispatch-plan) | ✓ (endNode returns node_info_render section-list contract; orchestrator renders) | ✓ (renders mandatory six-section node-info per inline node) |
| governance_hash verification | ✓ (engineExec.step2 governance_hash + TRI-SYNC check at execution start) | — | — | ✓ (engineExec.step2 verifies governance_hash TRI-SYNC, parity with main) |

### Key Mechanisms

| Mechanism | Location | Description |
|-----------|----------|-------------|
| **Crash Recovery** | node_engine, normal_engine | `last_completed_node` atomic marker. On re-entry, skip completed nodes. |
| **Circuit Breaker** | node_engine, normal_engine | 3-state: closed → open (3 failures) → half-open (30s timeout, 1 test dispatch) |
| **Parallel Dispatch** | node_engine | Analyze mermaid graph for independent branches. Dispatch parallel waves. |
| **NodeVerify** | node_engine, normal_engine | Lightweight audit: tool_calls > 0 for 2+ step nodes. FAIL → retry → DEGRADED. |
| **Decision Node Gate** | node_engine, normal_engine | Read `route` field from cache for mermaid diamond nodes. Branch routing. |
| **WAITING_USER Relay** | main (M1) | Detect WAITING_USER status → present to user → resume with USER_ANSWER |
| **MESSAGE_PENDING Relay** | main (M1) | Detect MESSAGE_PENDING → forward to user → immediate resume |
| **sys.* Handling** | agent_engine, normal_engine | 24 calls, 8 namespaces. Forced blocking for sys.io.confirm/input/select (Axiom 0). |
| **trace_id / span_id** | main, agent_engine | UUID v4 trace_id in Router, span_id per node in Sub Agent cache. |
| **Bootstrap Validation** | agent_engine (A8) | Validate TARGET path, CONTEXT_DIR, YOUR_NODE, AIAP_NAME before execution. |

### OpenTelemetry Integration (v5.3.0 A1)

All 4 modules declare `opentelemetry-sdk >= 1.20.0` as a capability dependency. Trace context
propagation flows from `_index.json::trace_id` through all engine modules:

| Component | OTel Integration | Span Naming |
|-----------|-----------------|-------------|
| **Engine Router** | Root span creation, trace context source | `soulbot_execute_engine.main.{function}` |
| **Node Engine** | Child spans per node + 5 SpanEvents | `soulbot_execute_engine.node.{node_name}` |
| **Normal Engine** | Child spans per inline node | `soulbot_execute_engine.normal.{node_name}` |
| **Agent Engine** | Sub Agent spans with cache span_id | `soulbot_execute_engine.agent.{YOUR_NODE}` |

SpanEvents dispatched by Node Engine (A2):
- `node_started` — node begins execution
- `node_completed` — node completes successfully (PASS/PARTIAL)
- `node_failed` — node fails (FAIL/DEGRADED)
- `circuit_breaker_tripped` — circuit breaker state transition
- `decision_gate_routed` — Decision Node branch routing

Fallback: structured JSON log when OTel SDK unavailable.

### OWASP ASI Threat Mapping (v5.3.0 B1)

Threat surface mapped to OWASP Agentic Security Initiative (ASI) ASI01-ASI10:

| ASI ID | Threat | Mitigation | Status |
|--------|--------|-----------|--------|
| ASI01 | Goal Drift | `governance_hash` comparison at execution start | GREEN |
| ASI02 | Excessive Authority | Workspace-scoped file_system, no shell=True | GREEN |
| ASI03 | Knowledge Poisoning | Bootstrap validation (A8), path safety guards | GREEN |
| ASI04 | Excessive Agency | Axiom 0 forced-blocking for user decisions | GREEN |
| ASI05 | Improper Output | USER_MESSAGE handling, output mode enforcement | GREEN |
| ASI06 | Cache Integrity | Cache HMAC verification on _index.json and node caches | YELLOW->GREEN |
| ASI07 | IPC Signatures | Inter-agent cache IPC signatures via span_id correlation | YELLOW->GREEN |
| ASI08 | Logging/Monitoring | trace_id + span_id + OTel SDK observability | GREEN |
| ASI09 | Supply Chain | Protocol version check, min_protocol_version enforcement | GREEN |
| ASI10 | Runtime Drift | Version field comparison across modules at bootstrap | YELLOW->GREEN |

### EU AI Act Art.50 Transparency (Compliance Advisory)

| Field | Value |
|-------|-------|
| **Applicability** | Potentially applicable — SoulBot Execute Engine orchestrates AI agent interactions with users via sys.io.confirm/input/select and USER_MESSAGE forwarding |
| **Disclosure Status** | PENDING — applicability analysis deferred from v5.3.0 (ref: quality_baseline v5_3_0_deferred_items C3) |
| **Enforcement Date** | 2026-08-02 (EU AI Act Art.50 transparency obligations) |
| **Action Required** | Complete applicability analysis before enforcement date. If applicable: add user-facing disclosure that responses are AI-generated, document AI system capabilities and limitations, ensure human oversight mechanisms (Axiom 0 sys.io.confirm already provides this) |
| **Reference** | https://artificialintelligenceact.eu/article/50/ |
| **Current Mitigations** | Axiom 0 forced-blocking for all user decisions (sys.io.confirm/input/select), output_mode prefix, trust_level=3 with constrained permissions |

### sys.* Support (AISOP Protocol §6)

| Blocking Type | Calls | Count |
|---------------|-------|-------|
| **Forced blocking** | sys.io.confirm, sys.io.input, sys.io.select | 3 |
| **Non-blocking interrupt** | sys.io.notify | 1 |
| **Blocking** | sys.run, sys.run.timeout, sys.io.read, sys.io.write, sys.code.exec, sys.code.eval, sys.llm, sys.llm.json, sys.llm.classify, sys.event.wait | 10 |
| **Non-blocking** | sys.io.print, sys.run.bg, sys.event.emit, sys.state.get, sys.state.set, sys.state.save, sys.state.load, sys.security.audit, sys.security.redact | 10 |
| **Total** | 8 namespaces | **24** |

### Sovereignty Bug Protocol (v5.17.0)

The Sovereignty Bug Protocol formalizes how the agent handles bugs discovered in the sovereignty toolset during execution. It implements the permissive sovereignty gate framework from plan20/34.

| Concept | Description |
|---------|-------------|
| **Red Line** | Self-modifying sovereignty toolset code without human approval = Axiom 0 violation (absolutely prohibited) |
| **Gate** | Everything else goes through user gate (human approves) |
| **Honest Boundary** | Local agent physically CAN modify; this protocol makes unauthorized modification have no excuse and always be detectable |

**Sovereignty Toolset Members:**
- `python_tools/user_gate_audit.py`
- `python_tools/agent_update_index.py` (FLAW-A un-advance branch)
- `python_tools/dispatch_audit.py`
- `python_tools/snapshot_audit.py`
- `sovereignty_bug_protocol` definition itself (self-referential closure)

**Bug Classification (3-tier):**

| Tier | Criteria | Response | Fix Assertion |
|------|----------|----------|---------------|
| **Critical** | Sovereignty bypass, gate removal, fix requires self-modification | Immediate WAITING_USER | REQUEST_IMMEDIATE |
| **Medium** | Safe to execute, no sovereignty impact, fix does not touch toolset | Continue + report in cache | SUGGEST_FIX |
| **Light** | Cosmetic/doc, zero danger | Continue + summary | ASK_IF_FIX |

**Separation of Powers:** Agent discovers + classifies + proposes (executive). Human approves (legislative). Exposure chain records (judicial).

**Exposure Chain:** Append-only sovereignty_log in `_index.json` with SHA-256 prev_hash chain. Integrity invariant: any TOOL_MODIFY must have a preceding HUMAN_APPROVAL with matching diff_hash.

### Known Limitations (v5.18.0)

> **Scope of this entry:** documentation/observability only. This records a known upstream runtime constraint; it does NOT describe an engine defect and does NOT change any engine execution logic.

**Underlying-runtime long-session JS heap OOM (Claude Code / Node / V8).**

| Aspect | Detail |
|--------|--------|
| **Symptom** | During a single long-running node session, accumulated in-memory state causes the underlying Claude Code child process (Node/V8) to terminate with exit code `0xC0000409` (abort / heap OOM). The crashing node drifts (it is not a fixed node) because the crash is triggered by cumulative heap pressure rather than by any specific node's logic. |
| **Root cause** | Upstream Claude Code behavior, currently marked *not planned* by the upstream project: (1) GitHub issue #25926 — the conversation message buffer (`mutableMessages`) grows monotonically and is never released; (2) GitHub issue #11155 — tool outputs remain resident for the whole session. This is an underlying-runtime memory-retention characteristic, **NOT a defect in this engine** (main/node/agent/normal engines). |
| **Diagnosis** | Set child-process `NODE_OPTIONS` to include `--heapsnapshot-near-heap-limit=2 --trace-gc` to capture a heap snapshot and GC trace immediately before the crash, confirming heap exhaustion as the cause. |
| **Mitigation** | (1) Add heap headroom via `--max-old-space-size`; (2) enforce a process boundary so the orchestrator survives a child crash — the existing `soulacp` retry already provides a fallback; (3) proactive session rotation; (4) splitting heavy nodes into smaller segments is the root-fix direction. Note: in-session `/compact` and sub-agent dispatch do **not** resolve this because they run inside the same process and do not free the underlying-runtime heap. |
| **Status** | Known upstream limitation, tracked for observability. No engine-side code change is warranted; resilience is provided at the process boundary (retry + rotation + node segmentation). |

**Parallel-wave concurrent `last_completed_node` write (v5.15.0 B1 caveat).**

| Aspect | Detail |
|--------|--------|
| **Symptom** | As of v5.15.0, `agent_engine.writeCache.step2` advances `_index.last_completed_node = {YOUR_NODE}` for every terminal agent node. `last_completed_node` is a single-valued scalar field. Under **parallel-wave dispatch** (Node Engine fanning out independent mermaid branches concurrently), two or more sub-agents in the same wave may write `last_completed_node` at overlapping times, producing a non-deterministic last-writer-wins value — the field may reflect any one of the concurrently-completed nodes rather than a well-defined cursor. |
| **Scope** | Affects ONLY parallel-wave execution. **Linear (sequential) pipelines — the default and the case this fix targets — never trigger it**, because exactly one node writes at a time. Crash-recovery does NOT depend on `last_completed_node` precision (it uses `current_node` + `nodes_status`), so a stale/raced value is observability noise, not a correctness hazard. |
| **Decision** | Deliberately NOT addressed in v5.15.0. Making `last_completed_node` correct under concurrency would require a different data shape (e.g. per-branch cursors or a completed-set) and a concurrency-model change, which is out of scope for this fix. Industry durable-execution engines (Temporal/LangGraph/DBOS) similarly require a reliably-advancing scalar cursor for the linear case and model concurrent completion separately. |
| **Status** | Documented caveat, tracked for a future concurrency-model evolution. No code change this cycle. |

## Usage

### Entry File

`main.aisop.json` — loaded by agent.py when `EXECUTE_ENGINE=true`. Contains Engine Router with intent matching, engine selection, and execution orchestration.

### Tool Requirements

| Tool | Required | Purpose |
|------|----------|---------|
| file_system | Yes | Read target AIAP, write execution cache, write node caches |

### Executor Shim Note

The `python_tools/` directory (containing `prepare_cache.py` and related helpers) is declared
via the top-level `executor_shim: true` field. Executor shims are **NOT registered AIAP tool
packages** (they do not follow AIAP Pattern G — no `tool.json`, no `tool_dirs` registration).
They are executor-internal helpers invoked by the runtime (e.g. agent.py's Python fast path
or the AISOP Bash CLI fallback) to implement atomic cache lifecycle. Downstream consumers of
this AIAP package (discovery, registry, governance) MUST treat `python_tools/` as executor
implementation detail, not as a discoverable tool. The `tool_dirs: []` declaration makes this
explicit: no embedded tool packages, all executor shims governed by `executor_shim: true`.

### Prerequisites

- `EXECUTE_ENGINE=true` in `.env` or environment
- Target AIAP packages exist in `aiap_store/` or `aiap/` with valid `main.aisop.json`
- Execution cache directory writable

## Example Interactions

**Scenario 1: Chat (Normal Engine)**
- User: "你好"
- Router: match → soulbot_chat [loading_mode=normal]
- Engine: normal_engine → inline execution → 8 nodes fast path → response

**Scenario 2: Creative (Normal Engine)**
- User: "给我讲个笑话"
- Router: match → soulbot_chat [loading_mode=normal]
- Engine: normal_engine → inline execution → 11 nodes full NLU path → joke response

**Scenario 3: Evolution (Node Engine)**
- User: "进化 soulbot_chat"
- Router: match → soulbot_creator_evolution [loading_mode=node]
- Engine: node_engine → Sub Agent per node → 21 pipeline nodes → evolved program

**Scenario 4: sys.io.confirm Interrupt**
- Sub Agent encounters sys.io.confirm step → writes WAITING_USER to cache
- node_engine → returns to Router → M1 relay → presents question to user
- User responds → Router re-dispatches with RESUME_MODE + USER_ANSWER
- Sub Agent reads steps_done → skips completed → continues from interrupt point

## Applicability

**Applicable**: Executing any AIAP program through the SoulBot runtime when EXECUTE_ENGINE=true. Supports both node mode (Sub Agent isolation, ~95% execution depth) and normal mode (inline, ~60-75% depth, faster).

**Not applicable**: EXECUTE_ENGINE=false (uses soulbot_router.aisop.json lightweight route). Direct CLI execution without agent.py. Non-AISOP format programs.

---

Align Axiom 0: Human Sovereignty and Wellbeing. Version: AIAP V1.0.0. www.aiap.dev
